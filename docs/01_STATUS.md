# 01 – Project Status

> **This document is updated every session.** It's the only place that shows where the project really stands.
> Status: 26.09.2026 · Stage 0 (Foundation) · Next gate: **G0 Go/No-Go** · Status version: 1.34.7

---

## Summary

**Menu from the register (T-4.11, 26.09.2026): the six `.dbf` copies become the import CSVs.** `python -m scripts.kasse_to_csv imports/kasse --out imports` reads the register tables read-only (own dBase reader, standard library, cp437 from the file header) and writes `menu_items.csv`, `item_options.csv`, `item_allergens.csv`. On the real files: 123 dishes, 751 options (soups as a required group klein/groß, extras such as a sauce change at 3,50 per dish group). Worked out on the real rows: `GRPREIS(n-1)` is the surcharge for size n (mask: klein 6,50, groß 11,00), size names 1 normal, 2 klein, 3 groß, 4 party from the register mask; extras hang on the dish group (`WRGSHOW`), their price is a price level from `zutgrp` (`ZPREIGRP3`); there is no lock column, the register marks a blocked article as deleted. Migration 004 `menu_items.pos_code` keeps the register spelling (`35B`), the import takes it as an optional column; `--deactivate-missing` sets dishes missing from the file inactive, dry-run lists them first. Not taken: 50 dishes whose numbers the search cannot resolve (`25G`, sushi `S1`...`SM6`), four register allergen entries (test entries, need `--allergens-confirmed-by`), size 6 without a name.

**Number sentences from eval suite v1 (26.09.2026): two phrasings resolved wrong in `sole_item_number`.** Building T-5.2 turned up two gaps in rule A. A leading "und" counted as a connector to a second number, so "Und noch die 24." - the normal way to add the next item - was a question back instead of item 24. And "würde", "hallo" and "dazu" were not filler words, so "Hallo, ich würde gern die 13" went to the name search. Fix per docs/08 §7: 14 lines added to `evals/cases/nummern.jsonl` first (9 red), then the rule: an "und" with no number before it is a continuation; between two numbers it still makes the sentence ambiguous ("die 23 und die 24"), and a leading "oder" stays a question. Review round on PR #147: the leading "und" only counts when it really opens the sentence - after a quantity ("zweimal und die 24") or a marker ("Nummer und 24"), or before a tens word ("und zwanzig", the cut-off half of "drei und zwanzig") it stays a question; the new filler words (plus "würd", "Guten Tag/Abend", "bestellen") are not skipped between marker and number and now also stand in `normalize.FILLER`, so the name search gets "pho" instead of "würde pho". Number eval 117/117. With the scripted model `abholung_0026` (Positionen nacheinander) and `abholung_0044` (Gericht im ersten Satz mit würde) turn green, no other case changes, and their `pending` field is removed: the suite is now 101/107 with six known gaps, hard metrics 0, false escalation 4.5 %. `abholung_0023` ("Zweimal die 23, ach nein, doch drei") stays pending: that is the quantity correction, not number recognition.

**Eval suite v1 (T-5.2, 26.09.2026): 107 cases, every mandatory line of docs/08 §6 covered.** Pickup (by number, name, alias, spoken numbers, quantities, options, ambiguous and sold-out dishes, unknown dishes, abort, read-back, closing time), reservations (times, dates, corrections at read-back, capacity, closed days), escalation (complaint, human, cancellation, noise) and allergens. A run takes about 12 s. With the rule-based stand-in model: 99/107 green, hard metrics 0, false escalation 4.5 %. Eight cases are **known gaps** (`pending` with the task that closes them: T-2.4 real model, T-6.5 delivery); CI demands every other case green and every gap red, so a gap that heals is noticed. The suite found four defects: two in the stand-in (a "yes" to the only offered dish was searched as a dish and asked again forever; a card number in the first sentence was stripped as a phone number), both fixed here; and one in the hot path, `check_slot` offering the previous day's times as alternatives (on closed Monday it offered Sunday 21:30 as "halb zehn"), fixed in its own bug PR; and the number rule for "und die 24" and "ich würde die 13", fixed in PR #147 (since then 101/107, six known gaps).

**"Gericht aus" on the tablet (T-4.8, 26.09.2026): the team takes a dish off, the agent stops offering it.** A button in the header (with "2 heute aus" next to it) opens a box above the columns with a search field and big switches. One tap marks a dish sold out until the end of the business day (05:00 local time), so it is back by itself the next morning; a second tap ("Wieder da") undoes it. Every tap is locked on the row and written to `audit_log`; other tablets follow over a new event `dishes`. `search_menu` then says "Frühlingsrollen ist heute leider aus. Stattdessen hätte ich Nummer 24 Sommerrollen." - up to two available dishes of the same category, never anything off the menu (D8). No migration: `sold_out_until` exists since 002. Clicked through in the browser against the eval menu.

**Kitchen ticket over a print bridge (T-4.6, variant B, 25.09.2026): a confirmed order reaches the printer, and the tablet knows whether it did.** The server sits in a data centre and cannot reach the receipt printer in the restaurant, and neither can n8n without opening the restaurant's router. So a small print bridge (`printbridge/`, Python standard library only) runs on a machine in the restaurant and fetches tickets over HTTPS (`POST /v1/kitchen/claim`, own token), prints them as ESC/POS on the Epson (network port 9100 or the Windows print queue for USB) and reports back (`/ack`). `handover_state` finally moves: `sent` when the newest revision is printed, `failed` at once on a print error or when no ticket was fetched for 60 s (watchdog in the dispatcher process, alarm, `order.handover_failed` to n8n), and back to `sent` when it prints after all. An outdated revision is never delivered after a newer one; a print log in the bridge stops a second slip when an acknowledgement got lost. Until today nothing ever set `sent` or `failed`, so no card could turn red. Tested against simulated printers (TCP with status query, Windows queue with stuck job); the first real print on the Epson TM-T20II needs Maxi on site. The dispatcher no longer sends `order.confirmed` to n8n. No migration.

**Eval runner (T-5.1, 25.09.2026): hard rule 4 has numbers now.** `make eval` plays every case from `evals/cases/` on its own throwaway database through the same conversation core as the text phone and judges the database, not the model's words. An observer between core and model counts the three hard metrics: a `menu_item_id` no search returned (guessed), a `confirm` without a preceding yes, and a missed escalation. Exit 1 on a crashed case, any hard violation, or a case that was green in the last passing run with the same model and tags and is red now (compared per case, not by accuracy). Reports go to `evals/reports/`; the whole suite also runs in CI. Today it measures the rule-based stand-in model; T-2.4 plugs in a real one and fills tokens and cost. 4/4 cases green, hard metrics 0. `/eval` and step 2 of `/bug` work from now on.

**Tablet column "Neue Bestellungen" (T-4.7, 25.09.2026): outside `primary` an order now reaches the kitchen.** Until today an order confirmed in `overflow`, `shadow` or `paused` waited for an approval nobody could give. The first column of the operations view lists every order the team still has to act on: confirmed today and not yet ticked off, every order still waiting for release (past 05:00 too), and every red card (kitchen not reached), even after "Passt" and past 05:00. "Passt" sets `approved`; outside `primary` it is the release and sends the ticket, in `primary` the kitchen already has it. A red card offers "Nochmal senden" instead. "Korrigieren" opens its own box above the columns (the column keeps updating and ringing): quantity, swap a dish by card number (quantity and note stay), add a dish, pick options as big buttons, save with one tap on the reason. Prices and totals come from the database on every tap. A note such as an allergy never disappears silently. Every correction is an `order.corrected` row in `audit_log` without personal data, raw material for the accuracy KPI. A ticket carries `revision`; a newer state overwrites a ticket nobody has tried to send, so an old state cannot overtake a correction (contract for T-4.6 in docs/04). Before building, the design went through a three-angle review (domain safety, concurrency, tablet UX) with adversarial verification; all 12 confirmed findings are built in. No migration: open PR #139 holds 003.

**Call log without recording (24.09.2026): real calls before the legal check.** Maxi asked to record team calls from tomorrow as learning data. Not possible yet: recording needs consent of customer and team (§201 StGB) and the full legal check (`docs/09_OPERATIONS_LEGAL.md`, all points open, T-7.x blocked); router, phone provider and handsets are not captured either (C1). Instead the team writes one line per call on a paper sheet (`docs/vorlagen/anrufprotokoll_druck.html`): time, intent, outcome, the customer's words verbatim, no names, no numbers. `python -m scripts.call_log imports/anrufprotokoll.csv` turns the typed CSV into the C1 baseline (volume, intent mix, outcomes, peak hours) and with `--cases` into eval case skeletons without any real customer sentence (`source: handcrafted`, empty transcript; the team re-enacts the sentences in its own words before a case moves to `evals/cases/`, DSFA M15). The script rejects the whole file if a free-text field looks like a phone number, e-mail, street address or a person's allergy (DSFA M8), and a semicolon inside text instead of shifting columns silently. Format and rules: `docs/17_ANRUFPROTOKOLL.md`.

CI enhancement (17.09.2026): The `docker-smoke` job builds the API image without local CA certificate and starts it without bind mount. It checks `/health`, rejection of missing/incorrect tokens, and an authenticated ping. This automatically catches earlier Dockerfile errors with certificate and package path. The test checks container startup, not DB integration; that stays in the pytest suite. Local Docker execution was not available in this session; execution via GitHub Actions.

The plan is set (`docs/00_PCF.md`, v1.0, released). The repo is set up and contains a **runnable minimal skeleton**: FastAPI with `/health`, token auth, response envelope, JSON logging, DB session, and the ten stage-1 tables per Alembic migration 001 with an idempotent seed with test configuration; hot-path tools (`get_service_status`, `check_slot`, `create_reservation`, `confirm`, `transfer_to_team`) respond in roughly 8–15 ms (p95); 295 tests passing. Reservation is complete: `create_reservation` creates the draft with `readback`, `confirm` makes it valid, writes `audit_log` and places the `reservation.confirmed` event in the outbox. The dispatcher (`api/events/`) empties the outbox: its own process, one POST per event to n8n with event id as idempotency key, backoff 5 s / 30 s / 2 min / 10 min, then `failed` with alarm in log. Callbacks are also done: `create_callback` creates the task for the team, logs it, and reports it via outbox. `transfer_to_team` hands escalations to the team: always the extension from `service_config.team_phone`, never a main number, and runs at most once per call (state on `calls.transfer_reason`). The call log is done: `POST /v1/calls/start`/`/end` open and close the `calls` row the rest of Stage 1 depends on, and every `/v1/tools/*` call now lands in `calls.tool_calls` with duration and result, written generically by `core/tool_log.py` so no individual tool needed to change. `prompts/system_v1.md` and `tools_v1.md` are also done, scoped to the six tools that actually exist today.

`agent/` now exists (T-2.1, 17.09.2026): `prompt.py` loads `prompts/system_vN.md` and can append a menu index once one exists; `state.py` is the compact per-call state from docs/05 §5 (`stage`, `intent`, `slots`, `open`) instead of a growing transcript; `dispatch.py` is the direct, non-HTTP equivalent of `api/tools/*.py` — validates arguments, calls `domain/` straight (no HTTP detour, per docs/11 §agent), auto-derives `idempotency_key` for `create_reservation`/`confirm` so the model never has to (CLAUDE.md §2 rule 1), and writes every call into `calls.tool_calls` itself since the HTTP middleware never runs on this path; `loop.py` drives customer-turn → model → tool-calls → reply, aborting cleanly on `max_call_seconds` or a runaway tool loop; `llm.py` defines the swappable `LLMClient` interface plus a `FakeLLM` test double — a real model with token counting is T-2.4. **Codex review PR #101 (P1×3, P2×2) fixed same-day:** `LLMTurn` gained an optional `state_patch` so conversational details the model extracts (name, date, party size, ...) survive into the next compact state instead of vanishing the moment the turn ends; `state.to_prompt_json()` now exposes `reservation_id` once a draft exists, so a stateless model can still supply `entity_id` to `confirm` on the customer's "yes"; the loop rechecks `max_call_seconds` before every tool hop, not only once at turn start; a tool's `say` is now forwarded to the model instead of dropped, so it isn't left to invent wording for cases the code already has a prescribed sentence for; and — the one that mattered most — hitting `max_call_seconds` or the tool-hop limit no longer just *announces* a handoff to the team and hangs up: it now actually calls `transfer_to_team`, falling back to `create_callback` when a phone number is known and the team is unavailable, and only falling back further to the plain apology when neither is possible (CLAUDE.md §2 rule 5).

**T-2.2 done (17.09.2026):** `agent/ladder.py` is the understanding ladder as a small state machine (docs/05 §2) — per-field failure counts (`UnderstandingLadder`), one level-up after two failed attempts at the same information, and `should_end_call()` once three level-ups pass without success; stages 4 (keypad) and 5 (SMS) are context-specific tools per docs/05 §2, not mandatory rungs, so Stage 1's reservation fields (date, party size, name, phone) only ever climb through 1–3 before the ladder gives up on its own. `agent/escalation.py` is the immediate-trigger check from docs/05 §4 (complaint, human-requested, cancellation) — plain keyword matching on the raw customer text, run in `loop.py` before the model is ever called, so an obvious case costs no tokens. Both are wired into `loop.py`: `LLMTurn` gained `understanding_failure` (the field name that failed) so the model only has to report failures, never count them; a ladder hint (`{"field": level}`) rides along in the compact prompt state once a field is elevated; escalation runs first in every `run_turn()`, ahead of even the `max_call_seconds` check; and every loop-internal handoff (timeout, hop limit, ladder exhaustion, or a genuine escalation trigger) now goes through the same `_handoff()` from the T-2.1 Codex fix, just with the real reason threaded through instead of a hardcoded placeholder. **Codex review PR #102 (P2×3) fixed same-day:** a repeated `understanding_failure` for the same field within one `run_turn()` call (e.g. reported once on a tool-call hop and again on the final reply) now counts once, not once per hop, so the ladder can't climb faster than the customer actually failed; `escalation.py`'s cancellation trigger now matches the noun "Storno" itself (`"storn"`), not only the verb forms; and `_handoff()` now threads the triggering customer text into the `create_callback` summary, since a pre-model escalation never calls the LLM and would otherwise leave the team a fixed phrase with no record of what the customer actually wanted. Still open at that point: the GUI.

**T-2.3 done (17.09.2026): the through-cut without a phone runs.** `sim/` is the text phone: `python -m sim.cli` holds a call in the terminal, `python -m sim.replay <case>.json` replays a transcript in the format from docs/08 §1, and both write to the real database — a confirmed reservation is in `reservations` afterwards, without any voice platform. `sim/session.py` holds what both entry points share: `start_call` opens the `calls` row, the compact state runs through `agent/loop.py`, every tool call is shown with the duration that `calls.tool_calls` recorded, and `end_call` derives the outcome from the conversation state (`confirmed` to `completed`, `transferred`, `callback`, otherwise `abandoned`) instead of from what the model claims. `sim/noise.py` garbles input on purpose — letter swaps, cut-off and swallowed words — with a seed, so a failure of the understanding ladder is reproducible instead of anecdotal. Because a real model only arrives with T-2.4, `sim/scripted_llm.py` stands in for it: rule-based, scoped to the reservation flow of `prompts/system_v1.md` (status, collect, `check_slot`, `create_reservation`, read back, confirm on a clear yes). It recognizes party size, day and time, name and phone number; what it does not recognize it reports as a failed attempt to the ladder rather than guessing (CLAUDE.md §2 rule 2), and a failed tool without a prescribed sentence makes it really hand over instead of announcing it (rule 5). Measured on the first case (`evals/cases/reservierung_0001_tisch_fuer_vier.json`): four customer turns, tool calls between 7 and 15 ms, reservation `confirmed` in the database. **Codex review PR #104 (P1x4, P2x2) fixed same-day:** a yes is now matched at word boundaries, so "Im Januar muss ich erst nachsehen" no longer counts as the explicit yes that turns a draft into a booking (CLAUDE.md §2 rule 3); an out-of-scope request (menu, pickup, delivery, allergy) is checked before the status call and remembered until the callback exists, because the follow-up turn usually carries nothing but the phone number and would otherwise drop the caller back into the reservation questions; a reply naming only a new time ("dann halb acht" to an offered alternative) keeps the day already given instead of rebooking the evening onto today; an impossible spoken date ("am 31.02.") counts as not understood rather than being silently replaced by today, and no longer escapes the conversation loop as a ValueError; and `finish()` closes the call at the current time instead of the start time, so a conversation from the terminal no longer records zero seconds (`--now` still pins the clock for a reproducible replay); and a correction during the read-back ("Nein, wir sind fuenf Personen") now builds a new draft with a new read-back instead of leaving the old one in place, where a later yes would have confirmed the four-person draft the customer had just corrected. The corrected-away draft stays in the database as a draft and blocks capacity until the cleanup job for abandoned drafts exists (already listed as missing above). 46 new tests, 341 in total.

**T-0.8 done (17.09.2026):** `api/domain/menu/numberwords.py` turns spoken German numbers into integers, as a pure function without database or HTTP. The speech recognition delivers the same number in four shapes - as digits ("23"), as one word ("dreiundzwanzig"), written apart ("drei und zwanzig") or spoken digit by digit ("vierzig sieben") - and all four now yield the same value. Three entry points, because they differ at the phone: `parse_cardinal` (the whole text is the number), `find_item_number` (the number sits inside a sentence; an explicit "Nummer ..." beats everything, and two numbers without a marker give `None` instead of the first one) and `find_quantity` (only with a marker: zweimal, 2x, drei Portionen - a bare number is a dish, not an amount). No match gives `None`, never a guess (CLAUDE.md §2 rule 2); what follows from that - a question back, a rung of the understanding ladder, a handover - the caller decides. 375 tests: a round trip over 0 to 199 and every seventh value up to 999 against a separately written speller, plus the cases that actually go wrong on the phone. `sim/scripted_llm.py` now uses this module instead of its own provisional table, so "einundzwanzig Personen" works where twelve used to be the ceiling. Measured at 3 to 5 microseconds per call. 716 tests in total. **Codex review PR #105 (P1x2, P2x1) fixed in a follow-up, since the review arrived after the merge:** a number no longer grows across a punctuation mark or across the article of an amount, so "Nummer 20, eine Portion" is item 20 with one portion and no longer item 21 with amount 21 - the rule for digit-by-digit speech ("vierzig sieben") reached straight through the comma; an article that *begins* a number now counts, so "die ein und zwanzig" is 21 instead of 20, where the "ein" was discarded as an article before anyone checked whether it opens a number; and a number attached to an amount marker is no longer counted as a second dish number, so "2 x die 23" resolves instead of asking the guest back for nothing. Scanning now carries the position of each number (`_Span`), which is what makes the last one decidable at all. 8 new tests, 724 in total.

**T-0.7 done (17.09.2026):** the seven slash commands in `.claude/commands/` were played through once and the CI steps run green. `ruff check`, `ruff format --check` and `pytest` against a real Postgres: the whole suite green, lint clean - the same three steps the `test` job in `.github/workflows/ci.yml` runs. Two defects came out of the play-through and are fixed: `/gate` pointed at a section "Gate Workflow" that `docs/15_README_STRATEGY.md` does not have (it is called "Ablauf bei einem Gate"), and CI only ran on pushes to `main` and `task/**`, so a push to the `dev/**` branch a web session develops on triggered nothing until the pull request existed. `api/tests/test_commands.py` keeps the commands honest from here on: every command from CLAUDE.md §6 exists, every file and directory a command names exists (except `evals/reports/`, which the runner creates and `.gitignore` excludes), every `make` target it calls exists in the Makefile, and no command carries an emoji (CLAUDE.md §8). Two commands stay blocked and that is expected: `/eval` and step 2 of `/bug` need `evals/runner.py`, which arrives with T-5.1; `make eval` fails with ModuleNotFoundError until then, now noted in the README under Known Issues. `/done` step 1 uses `make lint`/`make test`, which need a Docker daemon - this session had none, so the identical commands ran natively against local Postgres instead.

**T-4.4 done (22.09.2026): `get_item_details`.** `POST /v1/tools/get_item_details` answers questions about a dish the caller already picked: description, option groups and allergens for a `menu_item_id` that `search_menu` delivered. Allergens come only from `item_allergens`; without a maintained row the answer is `known: false`, never a guess and never "free of it" (CLAUDE.md §9). The sentence from the code that the team will call back is tied to the mandatory `allergen_question` flag in the request: the same tool also answers questions about options, and those must not end in a callback promise. Maintained codes are read in the order of the LMIV list so every call hears the same order, and `confirmed_at` is the most recent proof, as the local date of the tenant (CLAUDE.md §8). Options and the state "sold out today" now come from one shared building block (`domain/menu/items.py`) that `search_menu` uses as well, so both tools cannot drift apart. p95 around 16 ms with a local database.

**T-4.3 done (18.09.2026): `search_menu`.** `POST /v1/tools/search_menu` resolves what the caller said to a `menu_item_id` in the order of docs/04: number, then exact alias, then trigram over name and aliases. One result above the high threshold is `fuzzy_single`; otherwise everything above the low threshold is `ambiguous` with at most three suggestions and a question in `say` ("Meinen Sie Nummer 47, Ente knusprig oder Nummer 48, Ente süß-sauer?"); nothing is `not_found`. Thresholds come from `MENU_FUZZY_THRESHOLD_HIGH`/`LOW` (0.72 / 0.45). Only active dishes are found; a sold-out dish comes back with `sold_out: true` and "… ist heute leider aus." Options are grouped, default first. `normalize_query` in `domain/menu/normalize.py` strips filler, number and quantity words; `numberwords.has_item_number_marker` is new. **Rule A (Maxi, 21.09.2026), after fifteen rounds of Codex findings on number parsing:** search_menu only takes a card number directly when the whole sentence is exactly that one number. Besides the number, a marker („Nummer“/„Nr.“), filler words, hesitations, punctuation and one quantity („zweimal die 23“, „zwei Nummer 23“) are allowed - nothing else. Anything mixed in - a second number, a name, an „oder“ - is `ambiguous` with one question: ‚Welche Nummer meinen Sie? Bitte sagen Sie mir nur die eine Nummer.‘ Without a marker, a number next to a name stays a quantity („zwei Frühlingsrollen“) and the name search decides. One rule instead of a growing list of special cases: every sentence shape that is not unambiguously a number becomes a question back rather than a silent wrong dish (CLAUDE.md §2 rule 2). `numberwords.sole_item_number` carries the rule; `find_item_number_ref`/`find_marked_item_numbers` remain for the ladder. **Eval set (21.09.2026):** eight rounds of Codex findings followed, each a genuine wrong-dish risk, and two of them broke a form that had been right before - every single fix was correct on its own. The rules for marker, card suffix, quantity and connector word interlock, and single test functions do not show that. `evals/cases/nummern.jsonl` now holds all 64 known sentences with the outcome each must produce (`"23"` the card number, `"!23g"` spoken but not a valid card form, `"?"` ask back, `"name"` leave it to the name search); `evals/number_eval.py` runs them without database, HTTP or model in milliseconds (`make eval-nummern`), and `api/tests/test_evals_nummern.py` drives the same file in CI so a case is maintained in exactly one place. Measured against the commits before the last fixes, the set fails with 3 to 11 deviations - it is a real net, not a snapshot of today. Verification: pure tests (normalize, marker, numberwords) run locally, ruff clean; the DB and HTTP tests including p95 with 200 dishes run in CI.

**T-4.2 done as code (18.09.2026): the menu import.** `python -m scripts.import_menu imports/ --dry-run` reads the four CSVs per docs/14 and reports; without `--dry-run` it loads. `domain/menu/importer.py` splits into `parse` (reads and checks without a database, every rule from docs/14 plus the ones the schema needs: prices only as digits and comma, never via float; required option group with exactly one default; LMIV letters only, lowercase accepted and upper-cased; allergens without `confirmed_by` rejected) and `apply` (one transaction, file is the truth for every dish it lists). Price changes on existing dishes stay in the report until `--apply-price-changes`. Aliases from calls or by hand survive a re-import; only aliases from an earlier import are removed. Dishes in the database but not in the file stay untouched and are listed. A second run with the same files changes nothing and writes no audit entry. `domain/menu/normalize.py` holds the alias normalization that `search_menu` (T-4.3) will share. Test data is invented and lives in the tests; the real CSVs come from the chat (C1) into the git-ignored `imports/`. Verification: CI on PR #115 green, 916 passed. **Codex review PR #115 (P2x2) fixed same-day:** a new `confirmed_by` for the same allergen codes, or codes kept while others change, now re-stamps every row of that dish with the file's confirmer and time and shows up in the report (before, the database kept the old confirmer); a dry run with `--apply-price-changes` reports "würde übernommen" instead of "übernommen".

**T-4.1 done (18.09.2026): migration 002, the schema for stage 2.** Six tables per docs/03: `menu_items` (card number as text, unique per tenant, `sold_out_until` for "Gericht aus"), `item_options`, `item_allergens` (no row = no information; `confirmed_by`/`confirmed_at` mandatory), `item_aliases` (source `manual`/`call`/`import`, the same alias may hang on two dishes), `orders` and `order_items` (`menu_item_id` mandatory, unit price frozen, options as JSONB). `pg_trgm` is enabled with GIN indexes on `name` and `alias` for `search_menu` (T-4.3). The database checks what the code must never get wrong: amounts in cents >= 0, `total = items + fee`, quantity > 0, known status and handover values, a dish that was ordered cannot be deleted. `test_migration_001` is now pinned to revision 001; the model-versus-schema diff runs on head in `test_migration_002`. Verification: offline SQL of up and down checked, ruff clean; the DB tests run in CI. **Review finding on PR #114 fixed same-day:** `item_allergens.allergen_code` now only accepts the 14 LMIV letters (`A`-`H`, `L`-`P`, `R`); the model comment had claimed "A bis N", which would have rejected sulphites (O), lupins (P) and molluscs (R), and without a check an import typo would have been stored as a confirmed allergen. **Codex review PR #114 (P2x2) fixed same-day:** `order_items` carries `tenant_id` with composite foreign keys to `orders (id, tenant_id)` and `menu_items (id, tenant_id)`, so an order can never hold another tenant's dish; the model-versus-schema and downgrade tests migrate the scratch database themselves instead of relying on test order.

**Design pass on the operations view (18.09.2026, after Maxi's feedback on T-3.4):** checked in the browser at tablet width (1180 px), light and dark. Fixed: summary and notes were 13 px and badges 12 px (classes from the desktop mockup) - now 18 px as docs/06 §1 rule 3 demands; "Erledigt" in dark mode was white on light green (about 2:1 contrast) - `button.primary` now uses the surface colour as text; callback cards lead with a large time like the Heute cards, the reason badge next to it; complaints carry a wide red left edge instead of a thin frame; "Anrufen" and "Erledigt" split the card width evenly; the column headings Heute and Rückrufe show a count, which moves with the fragment. Phone numbers are still shown in E.164 without grouping - a correct grouping needs the area code length, left open.

**T-3.4 done (18.09.2026): the callbacks column is operable, the operations view is complete for stage 1.** Open callbacks appear as cards: time taken, reason in words (Beschwerde, Nicht verstanden, Will Mitarbeiter sprechen, Anderes Anliegen), phone, the one-sentence summary, **Anrufen** as a `tel:` link and **Erledigt** without confirmation (docs/06 §1 rule 6). Complaints always stand on top with a red frame, after them the oldest first. `domain/callbacks/board.py` holds the reads and `mark_done` (row lock, `done_by`/`done_at`, `audit_log` `callback.done`; a second tap or a second tablet writes nothing). The event stream carries a third signal `callbacks`, fingerprinted over the set of open ids rather than `updated_at` - the lesson from Codex on PR #111. A new card plays a tone generated in the browser (WebAudio, no audio file, works offline), complaints their own. Verification: CI on PR #112 green, 809 passed. **Codex review PR #112 (P1) fixed same-day:** tablet Safari unlocks Web Audio only inside a user gesture, so the audio context is now created in the first tap (`touchstart`/`click`) with a silent buffer, not when the first callback arrives - before, the tablet stayed silent.

**T-3.2 done (18.09.2026): the header is operable.** Four buttons under the status row: **KI pausieren** is the emergency stop (one tap, no confirmation, `call_mode` to `paused`, header turns grey), **KI einschalten** asks once (`hx-confirm`) and restores the mode from before the pause, **Lieferung aus/an** and **Wartezeit +15 / +30**. The writes live in `domain/status/config.py` as docs/11 names it: every switch locks the `service_config` row (`FOR UPDATE`) so two tablets tapping +15 at once give +30, and writes `audit_log` with actor `gui:tablet` and the value before and after. The router in `gui/` only picks the tenant and renders `fragments/kopfzeile.html`. The event stream now carries a second signal `header` (fingerprint: `service_config.updated_at`), so a switch on one tablet - or a change straight in the database - shows on every tablet within two seconds, without reloading the column Heute. A failed tap still swaps in the header with the red bar (docs/06 §5) instead of looking like nothing happened; an unreachable server shows the yellow bar. Verification: ruff clean, SSE tests (12) run locally; the DB-backed tests (domain and router, 27 new) need Postgres, which this session did not have - CI on PR #111 ran them: 782 passed, lint clean, docker-smoke green. **Codex review PR #111 (P2x2) fixed same-day:** the header fingerprint now carries the shown values (mode, delivery, both wait times) besides `updated_at`, because `updated_at` is set only by the ORM and a raw `UPDATE` in the database left the tablets stale; and `app.js` swaps an error response into the page only when it is HTML - `main.py` answers `HTTPException` and unhandled errors with the JSON envelope, which would otherwise have replaced the buttons or the list. Those now keep the view and show the red bar with "Nochmal versuchen" (`#stoerung` in `base.html`).

**T-3.1 and T-3.3 done (17.09.2026): the milestone "Durchstich ohne Telefon" is closed.** A reservation talked into the terminal is on the tablet a moment later, without a phone line. `api/gui/` is the operations view: `router.py` serves the page under `/gui/` and the column "Heute" as an HTMX fragment, `templates/` holds `base.html`, `betrieb/index.html` and `fragments/heute.html`, `static/app.css` carries the variables and classes of the approved mockup (`gui/mockups/admin-desktop.html`), and HTMX lies next to it locally - nothing is fetched from a CDN, the GUI works without internet (docs/06 §2). The header shows mode, delivery and wait times from `service_config`, never from the code; its buttons and live refresh are T-3.2 and are deliberately absent. `domain/reservations/today.py` is the data behind the column: confirmed reservations of the business day, ordered by time - a draft is not a booking (CLAUDE.md §2 rule 3) and does not appear on the tablet. `gui/sse.py` is the event stream: it polls a cheap change token (count plus newest `updated_at`) every two seconds and sends a signal, the page then fetches the fragment itself. Polling the database rather than an in-process channel on purpose - `sim/`, the dispatcher and later the telephony write from their own processes, and an in-process channel would never see their bookings. A dropped database yields a `problem` event and the yellow bar from docs/06 §5 instead of a dead stream. **Decision D6 is settled: Jinja2 and HTMX, no React** (rationale in docs/06 §2). Measured end to end: `sim/replay` confirms a reservation in one process, the event stream in the API process reports the change within about two seconds and the reloaded fragment shows guest, time and party size. **Codex review PR #108 (P1x1, P2x2) fixed same-day:** `/gui/*` now sits behind basic auth in `deploy/Caddyfile` with user and hash from the environment - the checked-in production stack proxied the view straight through before, so guest names, phone numbers and notes were open on the internet; `/gui/events` resolves the tenant in its own short-lived session instead of the request-scoped one, because a stream open for hours would have pinned a pooled connection for just as long and a handful of tablets could have starved the hot path; and after a database outage the stream now sends a signal on the first successful poll even when nothing changed, so the yellow bar disappears instead of hanging until the next booking. 23 new tests, 747 in total.

Before the first real call two things are missing, which Maxi supplies in chat: current state of operations (C1) and choice of voice platform (C2). Claude Code can start building immediately: everything not touching the voice platform is specified.

**Verified (16.09.2026):** `make up` builds the API image and starts Postgres and API, `/health` responds `{"status":"ok"}`, `make lint` and `make test` run in the container (T-0.1 done). External code review (Codex) delivered four findings on Docker setup, all fixed: CA certificate optional in build, package path `/app/api` preserved, `scripts/` and `evals/` mounted in container, README quickstart reduced to what runs today.

Full roadmap from here to the target state: section "Roadmap" below. Full details per stage: `docs/00_PCF.md` §5.

---

## Roadmap: where we are, where we're going

| Stage | Goal | Gate | Status |
|---|---|---|---|
| P Plan | Plan released | – | passed 11.09.2026 |
| **0 Foundation** | Clarify facts, rights, budget, vendors | G0 Go/No-Go | running – block "Skeleton" in `docs/07_WORKPACKAGES.md` |
| 1 Reservation through-cut | full chain once real (test number → AI → tool → DB → GUI) | G1 | open |
| 2 Pickup | menu understood reliably, order to kitchen correctly | G2 | open |
| 3 Delivery | address and zone without error | G3 | open |
| 4 Learning & shadow measurement | measure real error rate without customer risk | G4 | open |
| 5 Overflow operation | AI answers, only when team doesn't pick up | G5 | open |
| 6 Primary operation & ongoing | AI picks up first, team fallback, monthly review running | Monthly review | open, target state |

**We are here:** Stage 0, block "Stufe 2: Abholung" (`docs/07_WORKPACKAGES.md` Block 3) - pickup runs over HTTP from `search_menu` to the pickup code (T-4.1 to T-4.5 and `confirm` for orders done, 23.09.2026). Since 24.09.2026 the agent core uses the menu tools too, and a pickup order runs end to end in the text phone (caller ID, repeat-back, pickup code). Since 25.09.2026 the tablet releases, corrects and resends orders (T-4.7). Since 25.09.2026 the kitchen ticket is printed by a print bridge in the restaurant (T-4.6 variant B). Since 26.09.2026 wishes to a dish (T-4.10) and "Gericht aus" on the tablet (T-4.8) are done. Next: the first real print on site, see "What's next". The milestone "Durchstich ohne Telefon" for reservations is closed. **Where we're going:** Stage 6, ongoing operation with AI as primary intake and team as fallback.

---

## Gates in Detail

| Gate | Content | Status |
|---|---|---|
| P | Plan released | passed 11.09.2026 |
| G0 | Budget · Rights · Telephony path · Vendor chosen | open |
| G1 | Reservation through-cut, 20 test calls error-free | open |
| G2 | Pickup, evals on target, 0 guessed items | open |
| G3 | Delivery, zone check error-free | open |
| G4 | Shadow measurement, AI ≥ team baseline | open |
| G5 | Overflow operation, 2 weeks on target | open |

---

## What's Next

### In Claude Code (can start immediately, without vendor)
1. **First real print of the input slip** (T-4.6 variant B built 25.09.2026; since D2 on 26.09.2026 it goes to the main receipt printer at the register with the header "NICHT IN KASSE – bitte eingeben", that change is still to build): decide which machine in the restaurant runs `printbridge/` (the register PC with Python and pywin32, or a small computer of its own), check the printer port (USB or network, `printbridge/README.md`), set `KITCHEN_BRIDGE_TOKEN`, print a test slip with `python -m printbridge --test` with Maxi on site. Variant C (register intake) waits for the <POS Provider> answer
2. **`check_slot` alternatives from the previous day** (found by T-5.2, 26.09.2026): fix with a red unit test first (`/bug`)
3. **Card numbers of the register** (found by T-4.11): 50 dishes are not imported because the search only knows numbers up to 999 with a letter a to f - `25G`, `26G` ... `60G` (letter g) and sushi `S1` ... `S53`, `SM1` ... `SM6`. Extend `numberwords` and `importer._CARD_NUMBER` deliberately ("Nummer S zwölf", "25 G"), with eval cases; then the first real import: `python -m scripts.kasse_to_csv imports/kasse --out imports`, `python -m scripts.import_menu imports/ --dry-run --deactivate-missing`, read the report, then again with `--apply-price-changes --deactivate-missing`. Print `pos_code` instead of `number` on the input slip with the T-4.6 slip change
4. **T-3.5** the five-minute operating test on a real tablet with a team member (needs a person, not code; T-3.2 and T-3.4 done 18.09.2026)
5. **T-2.4** `agent/llm.py` against a real model with token counting; `sim/scripted_llm.py` is the rule-based stand-in until then and stays as the deterministic client for evals
6. An n8n workflow for the other events (`reservation.confirmed`, `callback.created`, `order.handover_failed` as a push or SMS to the team; export to `n8n/`); until then the cold path runs to nowhere, the kitchen ticket itself does not depend on it
7. Anytime in parallel: nothing open in Block 0 - T-0.7 (slash commands and CI) and T-0.8 (number words) are done

Sequence of first seven sessions: `docs/07_WORKPACKAGES.md` § recommended order.

Details and full list: `docs/07_WORKPACKAGES.md`. Mirrored on GitHub as issues: nine group issues per block (#5 through #13), 68 work packages as sub-issues, labels per stage. Status maintained here in docs, mirror follows.

### In Chat (Maxi)
- **C1** Current state: register, phone system, call volume, menu format, baseline measurement, legal check
  - Start the call log now (`docs/17_ANRUFPROTOKOLL.md`), two weeks give a first baseline. Still missing for any recording: handsets
  - **Phone line (26.09.2026):** Fritz!Box 6591 Cable on a cable line, three numbers; callers use one number only. Number of simultaneous calls unknown (contract or the provider's customer portal). Why it matters for C2: a forward done by the Fritz!Box takes two channels (in and out), so with two channels one forwarded call fills the line; the AI platform itself takes calls in parallel, the line is the bottleneck, not the number of handsets
  - **Register data (26.09.2026):** converter built (T-4.11), mapping in `docs/14_MENU_IMPORT_FORMAT.md` §Quelle Kasse. In the register: shorten the four names cut at 40 characters (32B, 35C, 38E, 15C) and the two extras cut at 16 ("Panierte Hühnerb", "Nudeln statt Rei"); remove the allergen test entries (13, 14, 35A, 15C) or confirm them; name size 6 in the mask if the team uses it (today nine dishes, 3,60 to 12,50 cheaper); Maxi ticks allergens per article (today none maintained, so the agent gives no allergen information at all)
- **C2** Research voice platform, evaluate, PoC on test number

---

## Open Decisions

| # | Question | Who | When needed |
|---|---|---|---|
| D1 | Voice platform | Maxi after C2 research | before T-2.x (integration) |
| D2 | ~~Handoff path to register~~ **decided 26.09.2026 (Maxi): receipt and kitchen ticket come only from the register** (<POS System>, TSE). An order is fed into the register, the register books it and sends receipt and kitchen ticket to its own printers. The print bridge stays as an **independent path** and prints on the **main receipt printer at the register** (80 mm), never in the kitchen: until a register interface exists it prints **every** confirmed order as an input slip headed "NICHT IN KASSE – bitte eingeben", the team types it in; with an interface (variant C) only when the register does not accept in time (rule 5). Planning assumption: <POS Provider> does not cooperate, orders are transferred by hand. Asking <POS Provider> stays open (interface, test mode, whether the unused register at Maxi's home may serve as a test system). docs/02 §2a, T-4.6 | Maxi | done; bridge changes open in T-4.6 |
| D3 | Hosting provider in the EU | C2 | before first deployment |
| D4 | Voice: natural or audibly synthetic | Maxi | Stage 1, dialog test |
| D5 | Delivery zones: postal code list or polygons | Maxi | before T-6.x (Stage 3) |
| D6 | ~~GUI tech: HTMX or React~~ **decided 17.09.2026: Jinja2 + HTMX** (docs/06 §2) | Maxi | done |
| D8 | ~~Wishes the menu does not know ("Nudeln statt Reis" without an option)~~ **decided 24.09.2026: no offer on the phone.** The agent offers only what is stored on the menu; surcharges come from `item_options` (e.g. a side-dish group "Reis 0 €, Nudeln +3 €"), the reason for a surcharge only from the menu data. Removal wishes ("ohne Karotten") are a note without price. Allergy wording before go-live with the legal check (docs/09). Built with T-4.10 on 24.09.2026 | Maxi | done |
| D7 | Does our own conversation core (`agent/`) also run in operations, or does the platform run its own loop? recommended: own core if the platform allows | Maxi with C2 | together with D1 |
| D9 | Appoint a data protection officer: the DSFA draft of 24.09.2026 rates the processing as DSFA-bound, which makes an officer mandatory regardless of team size (§ 38 Abs. 1 S. 2 BDSG); the DSFA also needs the officer's advice (Art. 35 Abs. 2). Maxi asked whether he can be the officer himself: likely not, the managing director decides purposes and means, would monitor himself (Art. 39) and report to himself (Art. 38 Abs. 3); Art. 38 Abs. 6 forbids a conflict of interest. Recommendation: external officer. Case law on this is not in the norm corpus | Maxi | before `shadow` with recording |
| D10 | ~~Deletion period for the call log CSV~~ **decided 24.09.2026: 90 days** (`CALL_LOG_RETENTION_DAYS=90`, weekly `scripts/call_log.py --frist-tage 90 --loeschen`, docs/03 deletion concept) | Maxi | done |
| D11 | ~~Register fields for the import~~ **decided 26.09.2026 (Maxi):** phone price is `VK1_PREIS` (regular; `VK2` pickup and `VK3` restaurant are optional and today equal) · `A_PREIS*` are promotion prices, not used · `GROESSE` is the dish size (soups small and large, different prices) · `DETAILS` (long description) is not used · allergen legend is the EU list, but the register letters run a to n **without gaps**, so from i on they differ from LMIV letters (i Sellerie = L … n Weichtiere = R); mapping table in docs/14. Allergens and additives are not maintained in the register yet, ingredients are | Maxi | done |

---

## Made Assumptions (subject to change)

- **`+` in `GROESSE` means "extras allowed"** (T-4.11): the converter offers register extras only for such dishes. Only fewer offers if wrong, never more. Extras price from `ZPREIGRP3`, not `ZPREIGRP` - all active extras fit, `Extra_Ente` has no `ZPREIGRP`; to be checked once in the dry-run report against the register. Assumption from 26.09.2026
- **Spoken allergy sentence for wishes is a draft** (`domain/menu/search.py` `SAY_ALLERGY_NOTE`, T-4.10): "Ihren Hinweis zur Allergie gebe ich an die Küche weiter. Ob … frei davon ist, kann ich Ihnen nur sagen, wenn es bei uns hinterlegt ist." No promise that a dish is free of anything; checked with the legal check (docs/09) before go-live. The kitchen note itself uses the fixed wording of E14. Assumption from 24.09.2026
- Python 3.12, FastAPI, PostgreSQL 16, Alembic, pytest, ruff
- GUI as FastAPI + Jinja2 + HTMX + SSE, one container, no Node build
- One location (<Pilotbetrieb>), but multi-tenant schema: each location-related table carries `tenant_id`
- German as the only language in stages 1–6
- Payment at pickup or delivery, no phone payment
- **Operations day begins at 05:00 local time** (`api/core/time.py`, `DAY_STARTS_AT`): an order at 00:30 counts toward yesterday. Not defined in any document, assumption from 16.09.2026, subject to change
- Domain logic errors respond with HTTP 200 in envelope so voice platform presents them to agent; only missing auth is 401
- **Test configuration in seed is placeholder** (`scripts/seed.py`): Monday closed, 11:30–14:00 and 17:00–22:00 for all services, capacity 30 (midday) / 40 (evening) guests in 30-minute slots, wait time 20/45 min. Real values come with C1 and then replace the seed
- Seed never overwrites `service_config`: live switch (mode, delivery, wait time) belongs to team
- **Capacity without dwell** (`domain/reservations/capacity.py`): `capacity.max_guests` is the sum of all guests whose reservation begins in the slot; slots represent seat turns (e.g., 18–20 and 20–22 hours). Drafts count, cancelled and soft-deleted don't. `check_slot` additionally requires an open `dinein` slot (special days override). Alternatives only same day, in `slot_minutes` grid, two next closest to request, never in the past. Not defined in any document, assumption from 16.09.2026, subject to change
- Open drafts block capacity until confirmed or cancelled; a cleanup job for abandoned drafts is still missing (candidate for `jobs/`)
- **`create_reservation` requires a known call** (`domain/reservations/create.py`): the `calls` row must exist before the first write, else `not_found` with outage `say`. No silent creation, so wrong IDs from platform surface immediately. Since T-1.9 (17.09.2026), `POST /v1/calls/start` is how that row gets created; before that it had to be created by hand or in tests. Assumption from 16.09.2026, subject to change
- **Draft checks the slot again** by the same rules as `check_slot`; booked → `conflict` with alternatives in `say`. Idempotency replay doesn't check again and returns the stored response; same key under a different tenant → `conflict`
- **Check and create are locked** (`_lock_business_day`): an advisory lock per tenant and day (`pg_advisory_xact_lock`) holds to transaction end, else two simultaneous calls read the same free capacity and overbook the slot. Coarse enough for phone load, fine enough that different days don't block each other. Same pattern used later by `confirm`
- **Readback format** (`domain/reservations/spoken.py`): "A table for four people today / tomorrow / on Tuesday the 22nd of September at half past six under the name Müller[, with the note: …]. Does that work?" Party size up to twelve as word, above that digit. Reference point for "today" and "tomorrow" is `created_at` of the draft, not current time, so a replay after midnight delivers the same sentence. Wording is suggestion, to be refined in dialog test (D4)
- **Phone numbers** are normalized to E.164 format in `domain/customers/phone.py`, default country `+49`; `0049…`, `0…`, spaces, slashes, and parentheses are resolved. Suppressed number not yet modeled (comes with `find_customer`)
- Each write writes `audit_log` (`actor: agent`, `action: reservation.draft_created` or `reservation.confirmed`), inline in domain code. Two places don't justify a shared helper yet; check again at three
- **`confirm` is idempotent over state, not key** (`domain/confirm.py`): a second call reads `confirmed` and responds the same way, even with different `idempotency_key`; a second outbox event never occurs. The key only goes in `audit_log`. This saves a separate key table; if later a transaction can exit `confirmed` state, this assumption flips. Assumption from 17.09.2026, subject to change
- **`confirm` requires the same call as the draft** (`domain/confirm.py`): `reservation.call_id` must match the calling `call_id`, else `not_found`. Otherwise one caller confirms the table of another guest, and the "yes" from rule 3 comes from the wrong person. Today this doesn't break anything because the draft UUID only comes from `create_reservation` of that same call. A later "guest calls back and confirms" would be its own path with its own check. Codex review PR #90 (P1), 17.09.2026
- **Simultaneous `confirm` calls are serialized per row lock** (`SELECT … FOR UPDATE` on the reservation), else two calls write two events to outbox and kitchen gets the transaction twice
- **`entity: "order"` is in contract but responds `not_found` until stage 2**: the branch in `domain/confirm.py` is where T-4.x adds the order. `pickup_code` stays `null` for reservations
- **`approved` (overflow operation) deliberately missing:** `RESERVATION_STATUSES` knows only `draft`, `confirmed`, `cancelled`. Approval flow comes with T-8.2
- **Backoff sequence and retry count** (`api/events/dispatcher.py`): 5 s, 30 s, 2 min, 10 min per docs/03, then `failed`. That's five delivery attempts total; `attempts` counts every attempt, including the successful one. Assumption from 17.09.2026, subject to change
- **Alarm is for now an ERROR line in log** (`dispatcher._on_failure`): no channel to team (phone, chat, GUI banner) yet. Once GUI is up, alarm goes there too. Assumption from 17.09.2026, subject to change
- **One event per transaction, fetched with `FOR UPDATE SKIP LOCKED`**: no event goes out twice even if two dispatchers run, and a slow HTTP call doesn't hold foreign rows. Order follows `next_attempt_at`, not arrival order
- **n8n treats event id as idempotency key** (header `X-Idempotency-Key`, docs/03 § outbox): a retried delivery after timeout must not create a second receipt in the kitchen. The workflow that handles this is still missing
- **Migrations no longer silence the app logger** (`db/migrations/env.py`): `fileConfig(..., disable_existing_loggers=False)`. Before, after the first migration in the same process every already-imported logger went silent, even in production after `alembic upgrade` from the same process
- **Lint rules live in `pyproject.toml`, not ruff default** (17.09.2026): without config, ruff version determines the rule set, and a Dependabot update fails red without code changes (PR #85, ruff 0.7 to 0.16: 14 violations). Set are `E, W, F, I, B, BLE, C4, UP, SIM, DTZ, RUF` without `E501` (formatter determines line length), plus `extend-immutable-calls` for `Depends` and friends because FastAPI needs the call in the default argument and B008 is not an error there. Verified with ruff 0.7.4 and 0.16.8, both green
- **Python stays 3.12, Dependabot jumps on the image are disabled** (17.09.2026): version lives in seven places (Dockerfile, CI, `pyproject.toml`, README badge and text, `CLAUDE.md`, this document, `CONTRIBUTING.md`); a PR that only bumps the Dockerfile (PR #82, 3.12 to 3.14) moves one and isn't tested by CI because CI doesn't build the image. 3.12 gets security updates through October 2028. The jump comes as its own work package when a dependency requires it or support end nears, moving all places together plus a `docker build` step in CI
- **`create_callback` is idempotent over state** (`domain/callbacks/create.py`): per call there's at most one open callback, a second call returns it. No `idempotency_key` in contract and no extra column; platform timeout must not leave the team two slips for the same guest. A completed callback (`done`) doesn't block a new one. **Check and create are per-call locked** (`pg_advisory_xact_lock`, like draft reservation): a lock on the callback row reaches for nothing because it doesn't exist yet. Contract: every future write path on `callbacks` takes the same lock, and database runs `READ COMMITTED` (set in `api/db.py` as of 17.09.2026 instead of server default) — under `REPEATABLE READ` the waiter doesn't see the foreign commit and creates a second one anyway. A second call does **not** update the existing callback: differing `summary`, `phone`, or `reason` are discarded. Codex review PR #95 (P1), red test first; plus a controlled overlap test (A holds lock, B waits and then reads A's callback) and a rollback test. No idempotency rule for this tool was set in docs/04, assumption from 17.09.2026, subject to change
- **The sim understands time by rule, not by model** (`sim/scripted_llm.py`): "halb acht" is 19:30, an hour below eleven means the evening ("um sieben" is 19:00), from eleven the number stands as said so the 11:30 lunch table does not become 23:30. A time without a day means today if it is still ahead, otherwise tomorrow. A day without a time is not enough for a reservation and counts as not understood. Assumption from 17.09.2026, subject to change and only relevant for the simulator — a real model (T-2.4) resolves this itself
- **The sim writes into the same database as the API** (`sim/session.py`): a call from the terminal is a normal `calls` row with normal tool calls, not a separate test path. That is the point of the through-cut, but it also means test conversations show up in the GUI and in reports later. A separate marker for simulated calls does not exist yet; candidate as soon as shadow measurement (stage 4) counts real calls. Assumption from 17.09.2026, subject to change
- **Read-back sentence after callback** (`SAY_NOTED`): "I've noted your number. The restaurant will call you back as soon as possible." Wording is suggestion, to be refined in dialog test (D4)
- **Bugfix in `normalize_phone` (17.09.2026):** a bracketed `(0)` after the country code was kept as digit, `+49 (0)7221 5551234` gave `+4907221 5551234` — a number that doesn't exist. Now it's dropped for international format and kept as leading zero for national. Found building T-1.7, red test first (`api/tests/test_domain_phone.py`)
- **"KI einschalten" restores the mode from before the pause, read from `audit_log`** (`domain/status/config.py`, `_mode_before_pause`): the pause writes `{from, to}` anyway, a separate column would be a second copy of the same fact. Without a readable pause entry (mode set to `paused` directly in the DB) it falls back to `shadow`, the mode in which the AI never picks up itself - never blindly to `primary`. Assumption from 18.09.2026, subject to change
- **Overturned 26.09.2026 by Maxi (from the demo): pickup and delivery wait separately, and both must be lowerable on the tablet** - new task T-3.6, docs/06 §3. Until built, the old behaviour below stands.
- **"Wartezeit +15/+30" raises pickup and delivery together, capped at 180 minutes** (`domain/status/config.py`, `MAX_WAIT_MINUTES`): the header has one button, not two, and a full kitchen is full for both (docs/06 §3). The cap stops endless tapping; the number itself is an assumption from 18.09.2026, subject to change. **There is no way to lower the wait time on the tablet** - docs/06 puts the base values in the admin view (§4), which does not exist yet; until then it needs a DB update. Revisit at T-3.5 if the team misses it
- **Browsers play the callback tone only after the first tap on the page** (`app.js`, autoplay policy): a tablet freshly loaded and never touched stays silent. In operation the first tap happens within minutes; check at T-3.5 whether a "Ton an" hint is needed. Assumption from 18.09.2026
- **"Erledigt" on a callback that no longer exists returns the fresh list, not an error** (`gui/router.py`): another tablet was faster or the page is stale, the current list is the right answer. Assumption from 18.09.2026
- **Card numbers are matched as text, without leading zeros** (`domain/menu/search.py`, Codex review PR #116, P1): "Nummer 23a" and "23 a" find `23a`, never `23`; "Nummer 07" finds `7`, "Nummer acht" finds `08`. Letters a to f only - an "x" after a number is the quantity marker. Two dishes with the same number after stripping zeros (`7` and `07`) give `ambiguous`. Number, suffix letter and "Nummer" marker always come from one spot in the sentence (`numberwords.find_item_number_ref`, Codex review PR #117, P1): "23a, nein, Nummer 23" is 23, and "Nummer" without a number right behind it ("Nummer weiß ich nicht") does not turn a quantity into a dish number. **The import enforces the same card-number format** (Codex review PR #117, P1): digits with optional leading zeros and at most one letter a to f; `23g`, `A12` or `12-3` are rejected with a clear message, because the search would otherwise silently resolve "Nummer 23g" to 23. docs/14 is updated - if the real menu uses prefixes like `A12`, the format has to be extended on purpose. Compact quantities ("2x", "2 Stück", "2 stk", "2 st") are stripped from the query like in numberwords (P2). Card numbers are capped at 999, the range `numberwords` reads, and stored lowercase so `23A` and `23a` are one number (Codex review PR #117, P2x2). Two different explicitly marked numbers in one sentence ("Nummer 23, nein, Nummer 24", "Nummer 23 oder Nummer 24") give `ambiguous` with both dishes, never the first one (P1). A suffix no menu can carry ("Nummer 23g", "23ab", "23 g") makes the spoken number invalid - `not_found` naming 23g, never dish 23 (P1); whether letters are glued to the digits is decided per token position, not per digit string. Connectors ("von", "vom") and a separated suffix ("die 23 a") count as part of the number expression; "Nummer 07 oder Nummer 7" is one number; a second number after a single marker ("Nummer 23 oder 24", "Nummer 23, nein 24") is an alternative and asks back - only when a word like oder, nein, sondern, bzw. or lieber connects it; "Nummer 23 mit 2 Soßen" or "um 12 Uhr" stays dish 23; a number directly after a candidate ("Nummer zwei drei", "Nummer 23 24") is also an alternative, and a correction word only links the number right before it ("Nummer 23 mit 2 oder 3 Soßen" stays 23); "und" and a bare comma also link ("Nummer 23 und 24", "Nummer 23, 24" ask back); a marked number out of range ("Nummer 1000 und 23") stays invalid instead of letting 23 move up; the import treats "7" and "07" as the same number, exactly as the search compares, while numbers before the marker stay quantities ("zwei Nummer 23"); an import upgrades an older "23A" row to "23a" instead of adding a second dish; if the tenant already holds both "23A" and "23a", the import stops with a clear message instead of hiding one (a person merges them); "Nummer 23x" and "Nummer 23 x" are invalid, not 23 (Codex review PR #117, P2x3). A spoken letter after a number word ("dreiundzwanzig a") is not supported yet. Assumption from 18.09.2026
- **`search_menu` never falls back from a spoken number to a name** (`domain/menu/search.py`): "Nummer 99" that is not on the menu is `not_found` with "Die Nummer 99 habe ich nicht auf der Karte", not the nearest-sounding dish. A number **without** "Nummer" next to other words is a quantity ("zwei Frühlingsrollen" is not dish 2, it searches "frühlingsrollen"); a number alone ("die 23") is the dish. Assumption from 18.09.2026
- **Exact alias match ignores filler words on both sides** (`domain/menu/search.py`): aliases are stored as written on the menu ("die knusprigen rollen"), callers add "bitte" or drop the article. The comparison runs in Python over the tenant's aliases (a few hundred), because `normalize_query` exists only in Python. Revisit if a menu grows past a few thousand aliases. Assumption from 18.09.2026
- **Menu import: the file is the truth for the dishes it names, not for the whole menu** (`domain/menu/importer.py`): options and import-aliases of a listed dish are synced, but a dish missing from the file is not deactivated - it may be referenced by orders, and a forgotten line should not silently take a dish off the phone. The report lists such dishes; deactivating stays a deliberate `active;nein`. An empty allergen line deletes previously confirmed codes ("no information" beats a stale value). Assumption from 18.09.2026
- **`orders.pickup_code` added, not in docs/03 before** (`api/models/orders.py`): docs/04 `confirm` returns it and docs/06 shows it large; docs/03 is updated. Generation (format, uniqueness per day) comes with `confirm` for orders in T-4.x. Assumption from 18.09.2026
- **Menu child tables carry no `tenant_id`** (`item_options`, `item_allergens`, `item_aliases`): they belong to exactly one parent row and reach the tenant through it, as docs/03 specifies per table. **`order_items` does** (Codex review PR #114): it has two parents, and with single-column foreign keys the database would accept a dish of tenant B in an order of tenant A; both are now referenced via `(id, tenant_id)`. The general rule "every business table carries tenant_id" is read as "every table with its own business key". Assumption from 18.09.2026
- **`orders.customer_id` has no foreign key until migration 003** (`customers` does not exist yet); `address_id` arrives with 003 as docs/03 plans. The downgrade of 002 keeps `pg_trgm` installed, because an extension is database-wide. Assumption from 18.09.2026
- **GUI writes require the `HX-Request` header** (`gui/router.py`, `_require_htmx`): basic auth from the proxy is sent by the browser even for a form on a foreign site, a custom header is not without a CORS grant. Cheap protection against forged taps without a form token. Assumption from 18.09.2026
- **E10 (set by Maxi, 24.09.2026):** The call log (`docs/17`) may be kept before the legal check is complete. The DSFA draft of 24.09.2026 still counts it as personal data (D12: time plus wording can be matched to the router's call list), so the rules in `docs/17` apply and a deletion period for the CSV is open for the data protection officer.
- **E12 (set by Maxi, 24.09.2026):** Recognising callers by `caller_id` is fine; the register's VoIP already keeps the customers. Legal basis per DSFA: Art. 6 Abs. 1 lit. f with objection right (Art. 21); the balancing of interests is still to be written. Recommendation from DSFA M10, not yet decided: the agent does not volunteer stored data (name, address, order history), a shared number can belong to someone else (R6).
- **E14 (set by Maxi, 24.09.2026):** A caller's own allergy is stored as a kitchen note on the item or the order, fixed wording "WICHTIG: Keine <Zutat>. Grund: Allergie", repeated in the read-back, never a promise that a dish is free of it (docs/05 §Allergie, docs/03 `order_items.note`, T-4.10). Maxi: no extra consent needed, the caller volunteers it and ignoring it could harm the caller and the business. Note for the data protection officer: the DSFA draft rates this as health data (Art. 9) and marks the legal basis "unsicher, Anwalt fragen" (V Frage 7); Art. 9 Abs. 2 lit. a asks for explicit consent for set purposes, other exceptions are not in the norm corpus. Open recommendation: remove the note text once the order is done, orders are kept for years (docs/03). The call log rule (docs/17, no allergies in the CSV) is unaffected
- **AI disclosure stays as E7 (24.09.2026):** Maxi asked whether the agent could reveal itself as AI only when asked, assuming the law is not adopted yet. Not taken over: the AI Act (VO (EU) 2024/1689, amended by VO (EU) 2026/1744) applies from 02.08.2026 (Art. 113), Art. 50 is not among the later dates, and Art. 50 Abs. 1 and 5 require the information at the first interaction at the latest. What is not adopted is the GDPR part of the Digital Omnibus (norm corpus, as of 08.09.2026), which does not touch Art. 50. The greeting stays short (`docs/05` §6). Lawyer confirmation welcome, the norm corpus is a non-official copy
- **E11 (set by Maxi, 24.09.2026):** Audio recordings and any further processing of them need the customer's consent. Maxi's view: transcribed text without audio may go into the repo. The code follows the stricter rule for now, DSFA M15 and CLAUDE.md §8 (real customer sentences never in git): eval drafts from the call log carry no real sentence. Loosening this needs the data protection officer's confirmation and a CLAUDE.md change.
- **E9 (set, 16.09.2026):** Everything runs on EU servers or with EU vendors, including transcription and analysis. Maxi's PC is only a development workbench.
- **`prompts/system_v1.md` covers Stage 1 only, not the full skeleton from docs/05 §1** (`prompts/system_v1.md`, `prompts/tools_v1.md`): docs/05's example skeleton includes a menu index and order-taking rules, but `search_menu`/`draft_order`/etc. don't exist until Stage 2 (T-4.x). Writing a prompt that references them would tell the model it can do something the code can't (CLAUDE.md §2 rule 1). Instead, unbuilt tools are named once as "not yet available" so the model routes those requests to `create_callback` (`reason: out_of_scope`) instead of guessing. `tools_v1.md` is a new file, not named in docs/05 — the task title asked for "tool descriptions for the platform" without specifying a format or filename, so it's versioned alongside the system prompt (`tools_vN.md`) rather than folded into `docs/04_API_TOOLS.md`, which stays the full contract (error codes, idempotency rules) and isn't meant to be handed to a model as-is. Token budget is checked with `len(text) / 4` as an approximation (~440 for v1 against an 800 budget) — a real tokenizer arrives with `evals/` (T-5.1); until then this is a rough but honest floor check, not the number a real model would report. Assumption from 17.09.2026, subject to change once D1 and `agent/prompt.py` (T-2.1) exist
- **`/v1/calls/start` and `/end` are our own contract** (`api/schemas/calls.py`, `domain/calls/`): docs/04 lists the two endpoints (§Endpunkte) but, unlike the `/v1/tools/*` tools, doesn't spec their request/response shape. Designed to match the existing conventions: `start` takes `tenant_id`/`external_session_id`/`caller_id`, is idempotent over `external_session_id` while a call with that id is still open (a platform retry gets back the same `call_id` instead of a second row); `end` takes the same `call_id`/`tenant_id` shape as the tool contracts plus `outcome`/`intent`/`cost_cents`/`model`, and is idempotent over state like `domain/confirm.py` (a second `/end`, e.g. a late "error" after an already-recorded "completed", reads back the first result instead of overwriting it). Both live directly in `main.py`, like `/health` and `/v1/tools/ping`, since they aren't part of the agent tool surface. Assumption from 17.09.2026, subject to change
- **`calls.delete_after` is 24 months from `started_at`** (`domain/calls/start.py`, `_delete_after`): docs/03 §Löschfristen names 24 months for anonymizing `calls` rows without personal fields, but doesn't say when that date is computed. Set once at call start rather than at `/end`, because the column is `NOT NULL` and a call must never sit without one. No cleanup job reads it yet (candidate for `jobs/`, docs/01 already lists a similar gap for abandoned reservation drafts). Assumption from 17.09.2026, subject to change
- **`caller_id` is normalized best-effort, never rejected** (`domain/calls/start.py`, `_caller_id`): a suppressed number stays `NULL`; anything else goes through `normalize_phone`, but if the platform's caller id isn't a phone number `normalize_phone` can parse, the raw value is kept as-is rather than failing the call. A call must never be lost over the log being imprecise (CLAUDE.md §2 rule 5). Assumption from 17.09.2026, subject to change
- **`calls.tool_calls` is written by a generic middleware, not by each tool** (`core/tool_log.py`): every `/v1/tools/*` response (except `ping`, which has no `call_id` and isn't a real tool) gets one entry `{name, duration_ms, ok, error_code?}` appended via an atomic `jsonb ||` update, keyed off the `call_id` in the request body. Best-effort: a broken `call_id` or a DB hiccup logs a warning and never changes the response the agent gets. Runs in the thread pool (`starlette.concurrency.run_in_threadpool`), not inline in the middleware's `async def`, so one slow write can't serialize concurrent tool calls on the event loop. Assumption from 17.09.2026, subject to change
- **`transfer_to_team` availability is derived from opening hours** (`domain/callbacks/transfer.py`): `available` is true whenever any service window (`dinein`/`pickup`/`delivery`) is currently open, false otherwise. There is no real reachability signal (a line ping on the extension) before the telephony adapter exists (T-1.11, after D1); this is the only fact in the DB today that says anything about someone being present. `available: false` doesn't fail the call — the agent falls back to `create_callback` per docs/05. Assumption from 17.09.2026, subject to change once the adapter can report a real answer/no-answer
- **`agent/dispatch.py` re-does what `api/tools/*.py` does over HTTP, directly** (T-2.1, docs/11 §agent: "`agent/` darf `domain/` benutzen, nie `tools/`"): validates arguments into the same Pydantic request schemas, calls the same `domain/` functions, and writes the same `calls.tool_calls` entry itself (via a new `core/tool_log.append_tool_call()`, factored out of the HTTP middleware so both paths share one SQL statement) — because that middleware only runs on the `/v1/tools/*` HTTP path and never sees calls the agent core makes directly. On an `AppError` it rolls the shared session back before logging, since (unlike the HTTP path's fresh session per request) this session lives for the whole call and a partial write from a failed tool must not ride along into the next one. `idempotency_key` for `create_reservation`/`confirm` is derived with `core/ids.idempotency_key()` from the call and its own arguments whenever the model doesn't supply one — the model must never decide it (CLAUDE.md §2 rule 1). Assumption from 17.09.2026, subject to change
- **`agent/loop.py`'s `_handoff()` defaults to reason `"not_understood"` for a self-triggered handoff** (`max_call_seconds`, too many tool hops, or the understanding ladder giving up — T-2.2): closest existing fit for "the loop itself gave up" rather than a genuine customer request; `TransferReason`/`CallbackReason` (docs/03) have no dedicated value for it and adding one is a schema change out of scope here. Superseded the earlier T-2.1 fix's `"human_requested"` default once T-2.2's `escalation.py` started passing real, customer-triggered reasons (`complaint`/`human_requested`/`cancellation`) through the same `_handoff()`, making the fallback default's own label matter for the cases nobody actually chose. `create_callback`'s `reason` falls back further from `"cancellation"` to `"human_requested"` when the real reason is passed through, because `CallbackReason` (docs/03) never included cancellation — it's a transfer-only reason (`api/schemas/transfer.py`), same gap noted in T-1.8's Codex fix. Assumption from 17.09.2026, subject to change
- **`agent/escalation.py`'s trigger words are a first pass, not a linguistic study** (`docs/05 §4`): plain substring matching on lowercased text for the three Stage-1-relevant triggers (complaint, human-requested, cancellation) — deliberately narrow (misses paraphrases, synonyms, negation like "keine Beschwerde") to keep false positives low, since a false trigger here skips the model and the ladder entirely. Widening it is an evals question (T-5.1: measure false-negative rate against real transcripts), not something to guess at from first principles now. `agent/ladder.py`'s field names (`party_size`, `reserved_for`, `guest_name`, …) aren't yet a fixed contract anywhere — they're whatever string a real `LLMClient` (T-2.4) chooses to report in `LLMTurn.understanding_failure`, matching whatever key it also uses in `state_patch`. Assumption from 17.09.2026, subject to change
- **Bugfix in `db/migrations/env.py` (17.09.2026, found building T-2.1):** `db/alembic.ini` sets `[logger_root] level = WARN`; `fileConfig()` applied that unconditionally, so any migration run in the same process as the app (every test through the `migrated_db_url` fixture) silently downgraded `api.core.logging.configure_logging()`'s INFO level to WARN for the rest of the process — dropping every later INFO-level app log line without any single test noticing locally. Only visible because `api/tests/test_agent_dispatch.py` and friends (alphabetically before `test_core_logging.py`) now run a migration before the two tests that actually assert on captured log output; before T-2.1 the file order hid it by luck. Fixed by saving the root logger's level before `fileConfig()` and restoring it after — alembic's own logger levels from the ini still apply, only the accidental root override is undone. Red before the fix: `test_core_logging.py::test_request_id_aus_header_wird_uebernommen_und_geloggt` and `::test_kontext_bleibt_nicht_am_naechsten_request_kleben`, run together with `test_agent_dispatch.py`

- **`draft_order` (T-4.5, 23.09.2026):** (a) `idempotency_key` is part of the request, as for every write tool (docs/04 §1), though the draft_order example lacked it. (b) A required option group without a choice is asked for, even when the menu marks a default - filling it in would be a guess (CLAUDE.md §2 rule 2). Required group = exactly one choice; optional groups allow several distinct options. (c) `ready_at` = now + `pickup_wait_minutes`, no load model yet (docs/11 `ready_time.py` stays unbuilt until there is a signal for load). (d) Limits 30 per position and 30 positions guard against mishearing, bigger orders go to the team. (e) Item order in the readback is kept via microsecond offsets in `order_items.created_at`, as the table has no position column - a column would cost a migration for no other gain today
- **`confirm` for orders (23.09.2026):** (a) Only `primary` sends an order straight to the kitchen; `overflow`, `shadow` and `paused` all wait for approval - docs/02 says so for `overflow`, and `shadow`/`paused` take no real calls, so an order there is a test and must not reach the kitchen by itself. (b) Pickup code format "A" + running number per business day, as in the mockup; the letter keeps it apart from a card number at the counter. (c) The response keeps `status: confirmed` also for `approved`/`handed_over` rows: for the caller it is confirmed either way
- **Tablet column "Neue Bestellungen" (T-4.7, 25.09.2026):** (a) "Passt" sets `approved` in every mode, not only in `overflow`: it means "the team checked it"; outside `primary` it is also the release. docs/03 is updated; T-8.2 makes the release switchable. (b) Corrections live in `audit_log` (`order.corrected`), not in a table of their own, and the ticket reuses `order.confirmed` with `revision` and `correction_reason` instead of a new event type - both would need a migration, and PR #139 holds 003. (c) The team may add a dish that is sold out today; it is marked "heute aus". (d) Card numbers are resolved like `search_menu` ("7" finds "07"), via a private import of `_by_number` until #139 no longer touches `search.py`. (e) A swapped or added position gets the base price of today's menu; a kept position keeps its frozen price; swapping only the options of the same dish keeps the frozen base price. (f) The correction box closes after 90 seconds without a tap. (g) Form fields are parsed with the standard library, not `Form()`, to avoid `python-multipart` as a new dependency.
- **Agent splits, HTTP asks back (23.09.2026):** the same multi-dish sentence gets two answers. Over HTTP `search_menu` answers `ambiguous` with "eins nach dem anderen", because a foreign voice platform may not know `split_positions`. The own agent core splits and searches per part, because it can: the caller does not have to repeat themselves. Both paths use the same `split_positions`, so they cut at the same places
---

## Open Points from Reviews

| Point | Why still open | When due |
|---|---|---|
| Register import, smaller review points (T-4.11) | Own review xhigh: `VK2_PREIS` 0.00 counts as a deviation (safe: dish left out, but an unset VK2 stored as 0 would drop it); the `--deactivate-missing` hint also shows after the flag was used; `WRGSHOWALL` true only as `T` (not `Y`/`t`); `pos_code` unique only per file, not in the DB; NUL-padded text fields would keep `\x00`; size names hardcoded in `pos_convert.SIZE_NAMES` although the register mask can rename them; `cents()`/`_eur` duplicate importer helpers; German parameter names in `convert()`; allergen-carrier warning only matches word starts. None changes a price or allergen the agent says today | with the card-number task or the first real register import |
| Register import, second review round (T-4.11) | Own review xhigh on the head: dBase III memo block size taken from bytes 20-21 instead of fixed 512 (the table version byte is not used); a dBase IV memo block without its marker falls back to the III scan instead of an error; the blanket IndexError catch in `pos_dbf.read_table` would report reader bugs as a corrupt file; exit 1 on every real run today (item errors), so `kasse_to_csv && import_menu` never chains; alias reading duplicates `importer._rows`. Real files read correctly (dBase IV, all markers present) | with the first real register import |
| Filler between leading "und" and a tens word (`api/domain/menu/numberwords.py` `sole_item_number`, `_opens`) | Codex PR #147 (P2, rule A: recorded, not blocking): the tens-word gate only looks at the token directly after "und". "und zwanzig" asks back, but "und äh zwanzig" and "und bitte zwanzig" return 20 - the cut-off half of "drei und zwanzig" with a hesitation in between becomes item 20. Fix: skip only punctuation, hesitations and non-number words after the "und" (never `ein`/`und`, they belong to a number), then read the whole number span there with `_scan`; ask back only if that span is a bare tens word. "und ein und zwanzig" scans as 21 and must stay 21 (existing line in `evals/cases/nummern.jsonl`, Codex PR #150). First as lines in `nummern.jsonl` ("und äh zwanzig", "und bitte zwanzig" -> "?") | with the next change to numberwords, latest before G1 |
| Wish and allergy in one sentence (`api/domain/menu/wishes.py` `_starts`) | Codex PR #139 (P2, rule A): "ohne Zwiebeln, ich habe eine Erdnussallergie" - the allergy clause wins, the removal before it is dropped. The allergy itself is kept. Fix: split the allergy clause off and keep the preceding wish as `note` | with the next wish change, latest before G1 |
| Two options in one wish (`classify_wish`) | Codex PR #139 (P2, rule A): "mit Huhn und Nudeln" is declined as `unknown` instead of two options from different groups. Nothing wrong enters the order, the guest names them again. Fix: accept unambiguous matches from distinct groups | with the next wish change |
| Allergy in the opening sentence (`sim/scripted_llm.py` `_after_customer`) | Codex PR #139 (P2, rule A): "Ich moechte Pho Bo mit Erdnussallergie zum Abholen" in the first turn matches `OUT_OF_SCOPE` before pickup is detected and creates a callback. Safe (the team calls back), but no order. Fix: detect pickup before the out-of-scope check. Stand-in only | with T-2.4 or the next sim change |
| Reason for a surcharge in the text phone (`sim/scripted_order.py` `_apply_wish`) | Codex PR #139 (P2, rule A): the cart keeps group and name of an option, not its `reason`; "Warum kostet das mehr?" is then searched as a dish. The real model reads `reason` from `search_menu` (prompt rule). Fix: keep `reason` in `CartItem.options`, answer the question before searching. Stand-in only | with T-2.4 |
| Number next to a wish in a list (`search_menu`, `has_number`) | Own test PR #139: "Die 23 ohne Karotten und die 13 mit Allergie" in the text phone ends in "nicht gefunden, Nummer?" - rule A holds the second number back. Nothing wrong enters the order. Fix: split positions before the wish check | with the next wish change |
| Spoken group for an option in two groups (`classify_wish`) | Codex PR #139 (P2, rule A): "mit Reis als Beilage" still returns `open` and asks "Reis bei Beilage oder bei Extra?" although the group was named. Nothing wrong enters the order, the question is one too many. Fix: filter duplicate matches by group-name words before `open` | with the next wish change |
| Denied allergy (`classify_wish`) | Codex PR #139 (P2, rule A): "ich habe keine Erdnussallergie" or "ich bin nicht gegen Erdnüsse allergisch" still gives the note "WICHTIG: Keine Erdnuss". Safe direction (the kitchen leaves something out), but a note the guest did not ask for; it is read back and can be corrected. Fix: check the polarity of the allergy clause | with the next wish change, latest before G1 |
| Option in two groups in the text phone (`sim/scripted_order.py` `_apply_wish`) | Codex PR #139 (P2, rule A: recorded, not blocking): when an option name exists in two groups, the domain returns `open` with `groups` and asks which one; the scripted model keeps no state for that question, so an answer like "Beilage" is searched as a dish and the option is not added. The order can still be completed. Fix: a pending group choice in `PickupScript`. Stand-in only | with T-2.4 or the next sim change |
| Rejection plus replacement in one answer (`sim/scripted_order.py`, question branch) | Codex PR #133 (P2, rule A 24.09.2026: recorded, not blocking): "keine davon, lieber Frühlingsrollen" closes the question and drops the whole answer, the caller has to name Frühlingsrollen again. Fix: short-circuit only a standalone rejection, search the rest of the reply. Stand-in only | with T-2.4 or the next sim change |
| Digit in a dish name against a number word in the alias (`sim/scripted_order.py` `_stated_quantity`) | Codex PR #133 (P2, rule A 24.09.2026: recorded, not blocking): a dish named `8 Schätze` found via the alias `acht schaetze` compares `8` with `acht` and reads 8 as the quantity. Wrong quantity shows in the readback, the caller can correct it. Fix: compare parsed cardinal values. Stand-in only | with T-2.4 or the next sim change |
| German identifiers in the older GUI code (PR #140 review, conventions) | CLAUDE.md §8 asks for English identifiers. T-4.7 code is English since PR #140 (`orders_fragment`, `correction_preview`, edit ops `swap`/`remove`/`restore`, `closeCorrection` in `app.js`); URLs, DOM ids and words the team sees stay German. Older GUI code from T-3.x still uses German names (`heute_fragment`, `rueckruf_erledigt`, `nachladen`) | rename in one refactor commit, no behaviour change |
| Swap to the same dish with the same options counts as a correction (`domain/ordering/correction.py` `_plan`) | Codex PR #140 (P2, after the fully worked round, rule 24.09.2026: recorded, not blocking): "Tauschen" with the original number and the original options is still classified `swapped`, so `NO_CHANGE` does not block. Saving writes `order.corrected`, raises `revision`, counts against the accuracy KPI and, if the kitchen has a ticket, sends a needless KORREKTUR. Fix: classify a line with the same `menu_item_id`, quantity and options as the stored row as `kept` | before T-8.4 reads corrections for the KPI, latest before G2 |
| `python -m printbridge --once` exits 0 after a failed run (`printbridge/bridge.py` `main`) | Codex PR #143 (P2, after the fully worked round, rule 24.09.2026: recorded, not blocking): the one-shot run is meant as an installation check, but the loop's catch-all logs the error and `--once` still returns 0, so a script cannot tell a failed check from a good one. Fix: return 1 from the `--once` branch after a caught error; daemon mode keeps retrying | before the first real print on site |
| Index for `outbox.payload->>'order_id'` (T-4.6) | Own review PR #143 (P2): `send_ticket` and the print bridge look up a ticket's newest revision by scanning the outbox for the order id; fine at pilot volume, but the outbox has no retention yet. An expression index needs a migration, and PR #139 is renumbering 003/004 | with the next migration after PR #139 |
| Machine for the print bridge and first real print (T-4.6) | The printers are Epson TM-T20II at the register ("Küche", "Receipt"); unknown yet whether USB or network. USB means the bridge runs on the register PC (Python plus pywin32, installing software there is Maxi's call because of the register vendor) or the printer is shared; network means any small computer in the restaurant. Code and tests cover both paths against simulated printers only | before the first test call with a real order |
| Scripted model takes a whole pickup order while pickup is closed (`sim/scripted_order.py`) | Eval suite T-5.2: at closing time or on the closed day the stand-in asks for dishes, name and phone and only `draft_order` says "Abholung ist gerade leider nicht möglich"; the outcome is right (nothing booked), the conversation is long. `get_service_status` already knows it is closed. The real model (T-2.4) gets it from the prompt | with T-2.4 |
| Turn-level assertions in eval cases (`evals/`) | Codex PR #145 (P2, after the fully worked round, rule 24.09.2026: recorded, not blocking): cases judge only the final database state, and `sim/replay.py` feeds every customer line unconditionally; the mandatory `pflichtoption` case would also pass if the agent never asked which Fleisch option is wanted and just consumed "Mit Huhn bitte". Fix: an optional per-turn expectation (a tool call or a question kind after a given line) in the case format | before T-5.3 model comparison |
| Store eval runs in `eval_runs` (T-5.1) | docs/08 §3 asks for it; the table needs a migration, and PR #139 is renumbering 003/004. Until then the reports in `evals/reports/` are the history, and the regression rule reads them | with the migration after PR #139, latest before G2 |
| Reminder for orders waiting for release (T-4.7) | Design review (UX, minor): an order outside `primary` only cooks after "Passt". Today one tone and a blink; after N minutes without release the tone should repeat and the card should say how long it waits. Needs a value in `service_config` | with T-8.2 (overflow approval flow) |
| Change the option of an existing position (T-4.7) | Design review (UX, minor): today "Tauschen" with the same number re-picks the options (base price stays frozen). A direct "Ändern" on an option line would be one tap shorter | at the T-3.5 operating test, if the team stumbles |
| Cancel an order from the tablet (T-4.7) | A correction may not remove the last position: an empty order is a cancellation, which needs a confirmation (docs/06 §1 rule 6) and a message to the kitchen. Not in T-4.7 | before G2 |
| Partial unique index on `callbacks (tenant_id, call_id) WHERE status = 'open' AND deleted_at IS NULL`, plus handling of uniqueness conflict as replay | Today exactly one path writes to `callbacks`, and it holds the advisory lock; the index would be the harder barrier but costs a migration | latest when a second write path to `callbacks` appears (GUI approval, import, jobs) |
| Lower the wait time from the tablet | docs/06 §3 only specifies +15/+30; base values belong to the admin view (§4), not yet built | at T-3.5 operating test, latest with the admin view |
| Reliable latency statement under lock contention | Measured ~11 ms is from same request repeated without concurrency; says nothing about lock wait times | with first load test, latest before gate G1 |
| **A sentence with two positions loses one of them - silently when no number marker is spoken** (`domain/menu/numberwords.py` `sole_item_number`, `domain/menu/search.py`) | Measured 21.09.2026 against the merged code, against the test menu of `test_domain_menu_search.py`. A number with markers, filler words and **one** quantity next to it is fine: `"einmal die Nummer 23 bitte"`, `"zweimal die 23"`, `"die Nummer 23a"` all resolve to `exact_number`. Two cases part ways when **content** stands next to the number. **With** a marker (`"die Nummer 23 und einmal Pho Bo"`, `"Nummer 23 mit Erdnusssauce"`) the answer is `ambiguous` and the agent asks for the one number - loud and safe, though a sauce is an option, not a second dish. **Without** a marker it is not a number sentence at all, so the name search runs over the whole sentence: `"die 23 und einmal Pho Bo"` comes back as `fuzzy_single` on Pho Bo (number 13) and the spoken 23 is gone without a word, `"einmal die 23 und zweimal Frühlingsrollen"` as `alias` on 23. Nothing is guessed, but one position is dropped in silence, and the agent has no signal that it happened. Fix belongs **before** the search: split the sentence per position, then ask `search_menu` once per position - that is order-flow work, not a change to Rule A | **Splitter built 23.09.2026** (`domain/menu/split.py` `split_positions`, T-4.5): the three sentences above now resolve to both positions, measured against the same test menu (`test_domain_menu_split.py`). **In the live path since 23.09.2026:** `search_menu` itself calls it and answers `ambiguous` ("eins nach dem anderen", parts in `message`) when a sentence names several positions, so the HTTP tool no longer drops one silently (Codex PR #124, P1). Still open: the agent has no menu tools yet, so nobody re-asks per part automatically. **Known limit, solved in the agent path (23.09.2026, Codex PR #127 P1):** a dish named without quantity, article or number after another position ("die 23 und Pho Bo") does not open a position by the sentence alone. `position_parts` in `domain/menu/search.py` then checks against the menu: if every piece at the separators hits a different dish on its own, they are several positions (HTTP asks back, the agent searches per part); otherwise the "und" belonged to a name ("Ente süß und sauer"). Left: two names where one piece matches loosely or not at all stay one search - the question-back of the understanding ladder has to catch that |
| **PR #120 (Google Jules, "CodeGuardian") on T-4.3** — decide and close | Opened 19.09.2026 against the pre-Rule-A branch. Its finding, an N+1 where `_by_number` ran once per parsed number, **no longer exists**: since Rule A (9d345a7) a sentence with more than one number raises `Ambiguous` before any query, and `_by_number` is called exactly once (verified on `main`, `search.py:171`). Merging the branch as it stands would reintroduce the multi-number path that Rule A removed. What is still worth a decision is the second half of that PR: a `.Jules/reviewer.md` journal of recurring bug patterns | after the gates, together with the deferred P2 polish |
| Anrufprotokoll `scripts/call_log.py`: Folgepunkte aus eigenem Review PR #135 | Nicht blockierend fuer den Start, bewusst nicht mehr in PR #135: (1) eine ungueltige `.env` (z. B. `CALL_LOG_RETENTION_DAYS=` leer) bricht schon beim Import mit pydantic-Meldung ab statt mit Exit 2, gilt fuer jede Komponente, die die Settings laedt; (2) die Meldung zu einer geaenderten Zeile hinter einem durchgesehenen Fall kommt nur einmal; (3) durchgesehene Faelle werden am Dateinamen erkannt, nicht am Feld `id`, umbenannt oder im Unterordner fallen sie heraus; (4) ein verwaister Fall kann doppelt gemeldet werden; (5) `numberwords` zieht ueber `api/domain/menu/__init__.py` SQLAlchemy mit, das Script braucht also die API-Abhaengigkeiten | nach den ersten zwei Wochen echter Protokolle, oder wenn einer davon im Alltag stoert |
| **#82 undecided** | Dependabot bump to Python 3.14, closed without merge. By convention it does not go to Done automatically. Maxi left the decision open; it is tracked in issue #136 (`projektpflege`), which the daily run keeps up to date. `PROJECT_TOKEN` was replaced on 24.09.2026, the first full run was green | when Maxi decides, via #136 or `/project` |

## Blockers

| Blocker | Blocks | Resolution |
|---|---|---|
| Legal check not complete | any processing of real call recordings (C7 / T-7.x) | work through `docs/09_OPERATIONS_LEGAL.md` |
| No vendor chosen | voice platform integration, real test calls | C2 in chat |
| <POS Provider> answer on order interface outstanding | Stage C of register integration (T-4.6 variant C) | send email, have license number ready; ask for test mode and whether the unused register at home may be a test system. Planned without it: manual entry from the input slip (D2) |
| Menu data not structured | Stage 2 complete | converter built (T-4.11, 26.09.2026); before the first real import the card number format must take `25G` and sushi `S12` (50 dishes) |
| Docker Hub rate limit on first `make up` possible (occurred 16.09.2026 once, cleared after wait) | image build | `docker login` with free account, see README, section Known Issues |

---

## Done

| Date | What |
|---|---|
| 26.09.2026 | **T-4.11 menu from the register:** `domain/menu/pos_dbf.py` (dBase IV reader with memo, read-only), `domain/menu/pos_convert.py` (sizes, extras, allergen letter table, report), `scripts/kasse_to_csv.py`; importer takes optional `pos_code`, `--deactivate-missing`; migration 004 `menu_items.pos_code`. Tests with synthetic `.dbf` only (`api/tests/dbf_fixture.py`), real files stay in `imports/kasse/` |
| 26.09.2026 | **Handover D2, D11, T-4.11 prepared** (PR #148, docs only): receipts only via the register, print bridge as input slip; register `.dbf` mapped in docs/14 incl. allergen letter table a-n to LMIV; next T-4.11 with the six register files. Full block in `docs/00_PCF.md` §13 |
| 24.09.2026 | **T-4.10 wishes to a dish (D8):** `domain/menu/wishes.py` splits dish and wish, `search_menu` returns `wish` - "die 23 ohne Karotten" finds the 23 again. Removal is a note, an option of the menu comes with its surcharge and reason from `item_options`, an allergy goes to the kitchen as "WICHTIG: Keine <Zutat>. Grund: Allergie" (E14) without a promise, anything else is not offered. The agent repeats the wish with the surcharge. Migration 003 `item_options.price_reason` (optional import column). Keys for `create_reservation` and `draft_order` from the validated request (two open points from PR #127 closed) |
| 24.09.2026 | **Pickup in the text phone** (branch `feat/sim-pickup-flow`, PR after #127): `sim/scripted_order.py` takes dishes, options and name, `draft_order`, read back, `confirm`, pickup code; eval cases `abholung_0001` and `abholung_0002`. **Caller ID** fills `slots.phone`, the agent no longer asks for the number (Maxi). **Repeat-back** of every clear dish right after it is said: a number as number, a description as the menu name with number, varied lead-ins chosen from the utterance (Maxi). Over HTTP a multi-dish sentence no longer asks the guest to repeat one at a time |
| 23.09.2026 | **Menu tools for the agent:** `agent/dispatch.py` runs `search_menu`, `get_item_details` and `draft_order` (plus `confirm` for orders) without HTTP. A sentence with several dishes is split with `split_positions` and searched per part; the answer carries one entry per part (`match_type: positions`), a part without a hit stays visible with `error_code` and `say`. `draft_order` gets its idempotency key from the call's data, so a model retry makes no second draft and a correction makes a new one. `ConversationState` tracks `order_id`, `draft_order` sets `readback_pending`. Prompt `v2` (`system_v2.md` ~630 tokens, `tools_v2.md`) is the default; its tests take the tool list from `dispatch.TOOLS`. 23 new tests (two of them from Codex: the entity read back last is the active one, the other id is cleared), suite 1492 green |
| 23.09.2026 | **Session handover (T-4.5, confirm for orders):** PR #124 and #125 squash-merged (`main` = `4610355`); pickup runs over HTTP from search to pickup code, 1470 tests green. Full block in `docs/00_PCF.md` § 13. Next step: menu tools for the agent |
| 22.09.2026 | **T-4.4 merged (PR #118):** `get_item_details` with the allergen rule. Review of this session found two issues, both fixed before the merge: the callback sentence fired on every detail lookup of a dish without maintained allergens, so a question about options was answered with a callback promise - it now hangs on the mandatory `allergen_question` flag in the request; and `confirmed_at` was the UTC date instead of the tenant's local date. The two Codex P1 findings were refuted, not fixed: they targeted the `search_menu` draft that `51192fc` removed, and main covers both through existing regression tests. 1359 tests green, p95 8,7 ms. main = 4933cee |
| 11.09.2026 | PCF v1.0 created and released, hybrid architecture decided (E1) |
| 15.09.2026 | Repo skeleton and specs exported for Claude Code |
| 15.09.2026 | API minimal skeleton: `/health`, token auth, response envelope, 3 tests green (T-0.3 done) |
| 16.09.2026 | README maintenance introduced: `docs/15_README_STRATEGY.md`, `/gate`, `CHANGELOG.md`, versions per gate; all emojis removed from bundle |
| 16.09.2026 | Rebranded as Maex Voice-Agent (product), pilot location maintained as placeholder `<Pilotbetrieb>`; locations in `docs/13` § 0 locked down: **nothing runs on Maxi's PC, not even transcription** (E9) |
| 16.09.2026 | Admin view as clickable desktop mockup in `gui/mockups/`, template for T-3.1 |
| 16.09.2026 | GUI round complete: operations and admin view as mockup approved, changes in `docs/06_GUI.md` § 7 |
| 16.09.2026 | Bundle v1.1: module architecture (11), playbooks + slash commands (12), deployment (13), menu import format (14), outbox, agent core + simulator, CI, prod compose; 18 new tasks |
| 16.09.2026 | README status synced to actual state |
| 16.09.2026 | Placeholders instead of names: `<Pilotbetrieb>`, `<Firmenname>`, `<Ort>`, `<Kassensystem>`, `<Kassenanbieter>`, example.com in code, docs, mockup, and Caddyfile; rule in `CLAUDE.md` § 1 |
| 16.09.2026 | **T-0.1 done:** `make up` against real Docker, Postgres + API healthy, n8n reachable (after fix `N8N_LISTEN_ADDRESS=0.0.0.0`), `/health` and `/v1/tools/ping` verified, `make lint` + `make test` in container green. Codex review (4 findings) incorporated. `ruff format` run first time (T-0.4: only `make migrate` left, waiting on T-1.1) |
| 16.09.2026 | **T-0.2 done:** `api/db.py` with engine (`pool_pre_ping`), `SessionLocal`, `get_db` (rollback on error, close always). Tests: session works, rollback on exception, DB unreachable returns `service_unavailable` envelope not stack trace. 7 tests green, local and in container |
| 16.09.2026 | **T-0.6 done:** `api/core/` with `envelope` (wrapper), `errors` (8 codes from 04 §1, `AppError` translates centrally), `auth` (extracted from `main.py`), `logging` (JSON lines with `request_id`/`call_id`, middleware measures duration, `X-Request-ID` taken or generated), `time` (UTC/local, operations day, DST-safe day boundaries). `main.py` uses only `core`. 34 tests green |
| 16.09.2026 | **T-1.1 done, T-0.4 done:** Alembic under `db/` (`alembic -c db/alembic.ini`, URL from `settings`), migration 001 with ten stage-1 tables including `outbox` and `audit_log`, enums as CHECK constraints, `idempotency_key` unique, `deleted_at` on reservations and callbacks. Models under `api/models/` (base, mixins for UUID PK, `tenant_id`, timestamps). Tests against throwaway DB: up, models no diff to schema, down/up, duplicate key, missing `call_id`, invalid `outbox.status`. `make migrate` runs, dev DB at 001. 42 tests green |
| 16.09.2026 | **T-1.2 done:** `scripts/seed.py` with `seed(session, tenant_name, timezone)` and CLI (`make seed`, JSON output). Idempotent: tenant and `service_config` only if missing, hours and capacity replaced deterministically. Shared throwaway DB fixtures in `api/tests/conftest.py`. Tests: counters, twice = same, live switch stays, second tenant, CLI. 47 tests green |
| 16.09.2026 | **T-1.3 done, T-0.5 done:** `domain/status/hours.py` (slots per day and service, special day beats weekday, slots past midnight, next opening) and `service.py` (`get_service_status`), schemas `ToolRequest` and `ServiceStatus`, tool router `tools/router.py` with `tools/service_status.py`. 16 tests: open, closed day with `say`, between slots, 00:30, slot 18–01 hours, special day closed, special hours on closed day, DST begin and end, delivery paused, unknown tenant, envelope, 401, `invalid_input`, `not_found`. **Latency:** p95 11.3 ms local, 12.3 ms in container (helper `p95_ms`, budget 300 ms), live via curl max 54.9 ms. Uvicorn access log off, middleware line has duration and `request_id`. 63 tests green |
| 16.09.2026 | **T-1.4 done:** `domain/reservations/capacity.py` (slots per weekday, booked guests from all slots in one query), `slots.py` (`check_slot`: `dinein` opening and capacity slot, up to two alternatives in grid, next closest first), `spoken.py` (spoken time "half past six"). Schema `CheckSlotRequest` (timezone-aware, `party_size ≥ 1`), tool `tools/check_slot.py`. 23 tests: free, full with alternatives, small party still fits, draft counts / cancelled doesn't, party bigger than any slot, closed day, slot end exclusive, no alternatives in past, past → `invalid_input`, naive time → `invalid_input`, spoken times. **Latency:** p95 13.0 ms local. 86 tests green |
| 16.09.2026 | **Handover sessions 1–3:** PR #2 merged (`main` = `df7c071`), handover block in `docs/00_PCF.md` § 13. Next step T-1.5. Sandbox stumbling blocks documented there (Docker daemon start manually, local `.env`, `DATABASE_URL` on localhost) |
| 16.09.2026 | **T-1.5 done:** `domain/reservations/create.py` (`create_reservation`: check tenant and call, normalize phone, re-check slot, draft + `audit_log`, idempotency replay also on concurrent double call via unique index), `spoken.py` extended with date ("today", "tomorrow", weekday) and party size, `core/ids.py` (`new_id`, deterministic `idempotency_key`), `domain/customers/phone.py` (E.164), schemas `CreateReservationRequest`/`ReservationDraft`, tool `tools/create_reservation.py`. 40 tests: draft with audit, replay no second transaction, foreign tenant, full slot with alternatives, closed day, past, unknown call, foreign call, unknown tenant, phone normalized/invalid, empty name, three read-back variants, draft counts against capacity, HTTP envelope, idempotency via HTTP, missing key, 401, 18 phone cases, key helper. **Latency:** p95 15.2 ms local, live via curl 3–4 ms warm. 129 tests green |
| 16.09.2026 | **Codex review PR #2 (2 findings) fixed, red test first:** `check_slot` considers previous day's slots past midnight (wish 00:30 in slot 18–01 was wrongly booked); `get_service_status` no longer counts paused delivery as open, promises pickup only with open pickup slot. 89 tests green |
| 17.09.2026 | **T-1.6 done:** `domain/confirm.py` (generic, `draft → confirmed`, row lock `FOR UPDATE`, `audit_log`, outbox event `reservation.confirmed` with full transaction in `payload`), schemas `api/schemas/confirm.py`, tool `tools/confirm.py`. 17 tests: confirm with audit and event, second call no second event, cancelled, soft-deleted, unknown, foreign tenant, unknown call, unknown tenant, `entity: order`, eight parallel calls lay exactly one event, HTTP envelope, idempotency via HTTP, invalid `entity`, missing key, 401. **Latency:** p95 9.9 ms (write path), live via curl 14 ms cold / 5.6 ms replay. 148 tests green |
| 16.09.2026 | Auto-update for this file: `scripts/status_bump.py` (semver, date, changelog line automatically), hooked into `/done`, `/task`, `/handover`; CI step "status sync check" fires if `docs/07_WORKPACKAGES.md` changes but this file doesn't; `ruff format` legacy in `api/main.py` fixed; `CLAUDE.md`: no Claude Code attribution badge in PRs/repo |
| 17.09.2026 | **T-1.8 done:** `domain/callbacks/transfer.py` + `tools/transfer_to_team.py`. Target is always `service_config.team_phone`, never a main number (no such concept exists in code); runs at most once per call via row lock on `calls` and state on `calls.transfer_reason` — a second call for the same call_id reads the same target without a second `audit_log` entry. Availability is derived from opening hours (any service window open now); a real reachability signal needs the telephony adapter (T-1.11, after D1) — assumption below. 17 tests: audit set, outside hours available:false with no state/audit written, a later real transfer in the same call still works after an unavailable attempt, second call no second audit, second call of a different call gets its own audit, unknown call/tenant/foreign tenant not_found, invalid reason rejected, `cancellation` accepted, eight parallel calls write exactly one audit. **Latency:** p95 well under 300 ms (write path comparable to confirm/create_callback). 203 tests green. Fixed `scripts/status_bump.py`: its header regex still matched the German originals (`Stand:`/`Status-Version:`) after this file's English translation, so it never matched the current header — updated to `Status:`/`Status version:`. **Codex review PR #98 (P1, P2) fixed:** `TransferReason` is now its own type including `cancellation` instead of aliasing `CallbackReason` (docs/05 names cancellation as a trigger, but it isn't a callback reason in docs/03); an unavailable `transfer_to_team` call no longer sets `transfer_reason` or writes `audit_log` — no transfer happened, so nothing should read as `transferred` later |
| 17.09.2026 | **T-1.9 done:** `domain/calls/{start,end}.py`, endpoints `POST /v1/calls/start`/`/end` in `main.py` (own contract, docs/04 lists the endpoints but not their shape — see assumption). `start` is idempotent over `external_session_id` while a call is still open, normalizes `caller_id` best-effort (never rejects the call over an unparseable one), sets `delete_after` to 24 months out. `end` is idempotent over state like `domain/confirm.py`. New `core/tool_log.py` middleware writes one entry per `/v1/tools/*` call into `calls.tool_calls` (name, duration, ok, error_code), atomically via `jsonb ||`, in the thread pool so it can't serialize concurrent tool calls; `ping` is excluded (no `call_id`, not a real tool). 27 tests: normalized/suppressed/unparseable caller_id, retry-safe start, idempotent end incl. a later "error" not overwriting an earlier "completed", cross-tenant and unknown-call/tenant not_found, invalid outcome rejected, 24-month retention math incl. leap-year Feb 29 edge, HTTP envelope and auth for both endpoints, successful and failed tool calls both recorded with the right fields, two calls append in order, unknown call_id in the body is a silent no-op. **Latency:** all existing tool p95 latency tests still pass with the middleware active. 230 tests green. **Codex review PR #99 (P1, P2) fixed:** the `tool_calls` update now filters on `tenant_id` as well as `call_id` — before, a request with a foreign `call_id` but the caller's own `tenant_id` could still get its result appended to a call it didn't own; `start_call` now takes an advisory lock per `(tenant_id, external_session_id)` before its check-then-insert, like `domain/callbacks/create.py` — before, two concurrent platform retries for the same session could each find no open call and create two, splitting subsequent tool activity across two `call_id`. 2 new tests (232 total): six concurrent starts of the same session yield one call, a foreign `tenant_id` in the body never writes into another tenant's call |
| 17.09.2026 | **T-1.10 done:** `prompts/system_v1.md` (~440 approx. tokens, well under the 800-token budget from docs/05 §5) and `prompts/tools_v1.md` — deliberately scoped to the six tools that exist today (`get_service_status`, `check_slot`, `create_reservation`, `confirm`, `create_callback`, `transfer_to_team`); Stage-2/3 tools (`search_menu`, `draft_order`, `get_item_details`, `check_delivery`) are named as "not yet available" rather than described, so the prompt never promises a capability the code doesn't have. 6 content tests (`api/tests/test_prompts_v1.py`, no DB): required sections present, every live tool named in both files, token budget held, no premature mention of unbuilt tools. Content-only task — no code, no migration; `agent/prompt.py` (T-2.1) is what actually loads this at runtime. 238 tests green |
| 17.09.2026 | **T-2.1 done:** `agent/` conversation core — `prompt.py` (loads `prompts/system_vN.md`, appends a menu index once one exists), `state.py` (compact per-call `ConversationState` from docs/05 §5: `stage`/`intent`/`slots`/`open`, plus `apply_tool_result()` to update it from a tool outcome), `dispatch.py` (tool name → `domain` function without the HTTP detour, one adapter per Stage-1 tool, auto-derived `idempotency_key` for `create_reservation`/`confirm`, writes `calls.tool_calls` itself), `loop.py` (`ConversationLoop.run_turn`: customer turn → model → tool calls → reply, aborts on `max_call_seconds` or after `MAX_TOOL_HOPS` without a reply), `llm.py` (`LLMClient` protocol, `LLMTurn`/`ToolCall`, `FakeLLM` test double). Refactored `core/tool_log.py`: the atomic `jsonb ||` update is now `append_tool_call()`, shared by the HTTP middleware and `dispatch.py` instead of duplicated. 27 new tests (prompt, state, dispatch against a real migrated DB, loop with `FakeLLM`, the `LLMTurn` contract itself): every Stage-1 tool dispatches, an unknown tool name and a missing required field come back `invalid_input` instead of crashing, a domain `AppError` rolls the shared session back and leaves it usable for the next tool call in the same turn, the loop feeds tool results back to the model, ends the turn on `transfer_to_team` success, and aborts cleanly on both stop conditions. **Latency:** `dispatch()` p95 5.5 ms for `get_service_status` over 30 calls (throwaway DB), comparable to the domain function alone. **Bugfix found while testing (see assumption below):** `db/migrations/env.py` no longer lets `alembic.ini`'s `[logger_root] level = WARN` silently downgrade the app's configured log level for the rest of the test process. 265 tests green. **Codex review PR #101 (P1×3, P2×2) fixed same-day** (see assumption below for the handoff-reason choice): `LLMTurn.state_patch` so extracted slot values survive to the next turn, `reservation_id` exposed in `to_prompt_json()` for `confirm`, `max_call_seconds` rechecked every tool hop instead of once, tool `say` forwarded to the model, and — the one that mattered — a timeout or hop-limit abort now really calls `transfer_to_team`/`create_callback` instead of only announcing a handoff and hanging up. 7 new tests. 272 tests green |
| 17.09.2026 | **T-2.2 done:** `agent/ladder.py` (`UnderstandingLadder`: per-field level/failure tracking, `record_failure`/`record_success`/`should_end_call`/`active_levels`) and `agent/escalation.py` (`check()`: keyword match for complaint/human-requested/cancellation from docs/05 §4). Wired into `loop.py`: `LLMTurn` gained `understanding_failure`; `run_turn()` checks `escalation.check()` on the raw customer text before anything else (no model call on a hit); a ladder hint rides along in `_prompt_state()` once a field is elevated; ladder exhaustion and every other loop-internal handoff (timeout, hop limit) now share one `_handoff()` with the real reason passed through instead of a fixed placeholder. 20 new tests (ladder state transitions incl. the level cap and independence between fields, escalation keyword matching incl. case-insensitivity, loop integration: escalation pre-empts the model, ladder exhaustion across six separate customer turns — not within one `run_turn` call — ends the call, a ladder hint appears in the prompt state, success clears a field's ladder state, `cancellation`'s callback fallback picks a valid `CallbackReason`). 292 tests green. **Codex review PR #102 (P2×3) fixed same-day:** `understanding_failure` deduped per `run_turn()` call so a failure reported on both a tool-call hop and the final reply in the same turn only counts once; `"storn"` added to the cancellation trigger so the noun ("Storno meiner Reservierung") matches, not only verb forms; `_handoff()` now passes the triggering customer text into `create_callback`'s summary. 3 new tests. 295 tests green |
| 17.09.2026 | **Session handover (T-2.1, T-2.2):** PR #101 and #102 squash-merged to `main` (`main` = `ea30937`), branch reset to `main` after each merge to keep diffs small; full block in `docs/00_PCF.md` § 13. Next step T-2.3 |
| 22.09.2026 | **`make test` and `make lint` match CI again.** The `api` service in `docker-compose.yml` mounted only the Python packages, so the container was missing everything the suite reads besides code: without `./sim` the four sim tests could not import `sim` (4 collection errors) and ruff sorted `sim` imports as a third-party package (3 false `I001` in `test_sim_cli.py`/`test_sim_replay.py`); without `./Makefile`, `./.claude`, `./deploy`, `./CLAUDE.md`, `./README.md`, `./CHANGELOG.md` and `./docs`, `test_commands.py` (3 tests) and `test_gui_router.py::test_caddy_schuetzt_die_gui` failed with `FileNotFoundError` on `/app/...`. All of it was a mount artifact, no line of application code was involved — the same suite has been green in CI the whole time, because CI runs on a full checkout. The paths are now mounted, read-only wherever the container only reads them (`./docs` stays writable because `scripts/status_bump.py` writes `docs/01_STATUS.md`). Of `.claude/` only `commands/` is mounted: in the main checkout the directory also holds `worktrees/` with full repo copies and their own `.env` files, which have no business inside the container (CLAUDE.md §8), and the test reads nothing but the commands. `dispatcher` needs none of them: it runs neither pytest nor ruff, only `api.events.dispatcher`. Verified on a fresh stack, after merging T-4.4 from `main` into the branch: `make lint` clean, `ruff format --check` 144 files, `make test` 1359 passed in 241 s. `pytest -q` (the CI command) and `pytest api/tests` collect exactly the same set — there is no test file outside `api/tests/`. **Guarded against the next time:** `api/tests/test_compose_mounts.py` parses the compose file and checks the api service's mounts against what the suite actually reads — every file and directory the slash commands name, and every path a test opens through `REPO_ROOT`/`parents[2]`. A new reference to an unmounted root file (say `CONTRIBUTING.md`) now fails in CI, where it is visible, instead of only in the container. Proven by mutation: dropping the `./deploy` mount and adding a `CONTRIBUTING.md` reference to a command file makes exactly those two tests red. It also pins the two decisions that are easy to undo by accident — `dispatcher` keeps its three mounts, and `.claude` is never mounted whole, because in the main checkout it holds `worktrees/` with other checkouts' `.env` files (CLAUDE.md §8). 1364 tests |

| 23.09.2026 | **T-4.5 done (pickup):** `domain/ordering/` after the blueprint in docs/11: `validation.py` (pickup open, item active and not sold out, options exist on this item, required groups chosen exactly once - the default is asked for, never filled in), `pricing.py` (cent sums), `readback.py` (one sentence per position, sum, pickup time, name), `draft.py` (draft + `order_items` with frozen card price + `audit_log`, idempotent per key incl. concurrent double call, `ready_at` from `pickup_wait_minutes`, warning `ready_after_close`). Delivery answers `invalid_input` until T-6.5. Tool `POST /v1/tools/draft_order`, schema `api/schemas/orders.py`. `domain/menu/split.py` splits a sentence per position before `search_menu`. 50 new tests; **latency p95 22.2 ms** in container; full suite 1414 green |
| 23.09.2026 | **`confirm` for orders:** `domain/ordering/confirm.py`. Row lock, same call and tenant as the draft, cancelled gives `conflict`. Pickup code A1, A2, ... per tenant and business day (day from the order's creation time, advisory lock per tenant and day, so concurrent confirmations never share a code). Only mode `primary` hands over at once (`handover_state` pending, `order.confirmed` with the full order in the outbox, labels from the draft snapshot); every other mode answers `awaiting_approval` and emits nothing until approval on the tablet (T-4.7). 20 new tests incl. three concurrency tests (the third one from Codex P1: `confirm` reads the mode `FOR SHARE`, so a running emergency stop is not overtaken); latency p95 36 ms; suite 1470 green |
---

## Versioning of This File

Own, semantic version `MAJOR.MINOR.PATCH`, independent of the `CLAUDE.md` bundle version and of README gate version (`docs/15_README_STRATEGY.md`) — README versions the product outward, this file versions itself inward:

| Bump | Trigger | Example |
|---|---|---|
| **MAJOR** | Gate passed / stage transition / architecture decision (E-Nr) flipped | G0 passed → stage 1 begins |
| **MINOR** | Decision made (D-Nr answered), new work package result changes "What's next" | D1 vendor chosen |
| **PATCH** | pure status housekeeping: task check, new assumption, new/resolved blocker | T-0.1 to done |

**Auto-update:** `python scripts/status_bump.py <patch|minor|major> "<one-line change>"` sets date, version, and changelog line automatically. Called by `/done`, `/task`, `/handover` (see `.claude/commands/`) — by hand only if needed. Extra safety net: CI (`.github/workflows/ci.yml`) fails if `docs/07_WORKPACKAGES.md` changes but `docs/01_STATUS.md` stays unchanged in the same diff.

---

## Changelog

- **v1.34.7 · 26.09.2026:** T-4.11 Review 2: Schutz gegen leere Karte, Aliase kommen zurueck, Memo nur aktiver Zeilen
- **v1.34.6 · 26.09.2026:** T-4.11 Review: Schalter --deactivate-missing, Gratis-Extra, verwaiste Aliase, kaputte Dateien
- **v1.34.5 · 26.09.2026:** T-4.11 done: Kassen-.dbf -> CSV, pos_code (Migration 004), --deactivate-missing
- **v1.34.4 · 26.09.2026:** Offener Punkt aus PR #147
- **v1.34.3 · 26.09.2026:** PR #147 Review: fuehrendes und nur am Satzanfang, Fuellwoerter auch fuer die Namenssuche
- **v1.34.2 · 26.09.2026:** Nummernsatz: fuehrendes und, wuerde/hallo/dazu als Fuellwort (Befund T-5.2)
- **v1.34.1 · 26.09.2026:** PR #148: Codex-Befunde - pos_code als eigenes Feld, fehlende Kassenartikel inaktiv, Import mit --apply-price-changes
- **v1.34.0 · 26.09.2026:** D11 entschieden: VK1 am Telefon, Groesse, Allergen-Umsetztabelle a-n auf LMIV, sechs Kassendateien fuer T-4.11
- **v1.33.0 · 26.09.2026:** D2 entschieden (Bons nur ueber die Kasse, Druckbruecke als Eingabezettel), Kassen-.dbf zugeordnet, D11, T-3.6 und T-4.11 angelegt
- **v1.32.1 · 26.09.2026:** T-5.2 done: Eval-Suite v1 mit 107 Faellen
- **v1.32.0 · 26.09.2026:** T-4.8 done: Gericht aus am Tablet, Alternativen derselben Kategorie im say von search_menu
- **v1.31.24 · 25.09.2026:** PR #139: eigenes Review, 13 Befunde behoben (Allergie bei neu gesuchter Wahl, mehrdeutiges Gericht mit Allergie, allgemeine Allergie fragt nach, "vertrage alles", Bestellung statt Zutat, Schluessel in E.164/UTC, Grund bleibt bei alter Optionsdatei, Schema der Wunsch-Felder)
- **v1.31.23 · 25.09.2026:** PR #139: letzte Codex-Runde - jede Allergie in "Nuss- und Sesamallergie" (P1), Intoleranz, wiederholte und bindestrichlose Allergien, ganze Antworten auf "Wogegen" behoben; sieben P2 als offene Punkte (Regel A)
- **v1.31.22 · 25.09.2026:** T-4.10 done: Wuensche zu einer Position, Migration 003 price_reason
- **v1.31.21 · 25.09.2026:** Offener Punkt aus PR #143
- **v1.31.20 · 25.09.2026:** T-4.6 B: Codex-Befunde PR #143 behoben
- **v1.31.19 · 25.09.2026:** T-4.6 B done: Kuechenbon ueber Druckbruecke
- **v1.31.18 · 25.09.2026:** T-5.1 done
- **v1.31.17 · 25.09.2026:** offener Punkt aus PR #140
- **v1.31.16 · 25.09.2026:** T-4.7 done
- **v1.31.15 · 24.09.2026:** PR #133 gemergt, ein Befund nach der abgearbeiteten Runde als offener Punkt (nur Text-Telefon)
- **v1.31.14 · 24.09.2026:** Anrufprotokoll: Nr. nur hinter Strassennamen gestrichen, doppelte Zeile zaehlt einmal, Datum im ID-Verzeichnis geprueft, Dateirechte bleiben (Codex)
- **v1.31.13 · 24.09.2026:** Anrufprotokoll: Fehlalarme bei Wochentag, Datum, Uhrzeit und Kartennummer behoben, Komma trennt gesprochene Zahlen; Folgepunkte aus eigenem Review notiert
- **v1.31.12 · 24.09.2026:** Anrufprotokoll: ID-Verzeichnis laeuft auch ohne alte CSV-Zeilen ab, kaputtes expected als Meldung (Codex)
- **v1.31.11 · 24.09.2026:** Anrufprotokoll: Nr./Nummer vor der Hausnummer, unlesbares Salz als Meldung statt Absturz (Codex)
- **v1.31.10 · 24.09.2026:** Anrufprotokoll: Hausnummern ab tausend, kaputtes Salz bricht ab, Loeschen ersetzt die CSV atomar (Codex)
- **v1.31.9 · 24.09.2026:** Anrufprotokoll: getrennte Zahlwoerter in Adressen, nachgetragene Anrufe derselben Minute, unlesbares ID-Verzeichnis (Codex)
- **v1.31.8 · 24.09.2026:** E14: eigene Allergie als Kuechenhinweis "WICHTIG: Keine <Zutat>. Grund: Allergie" (docs/05, docs/03, T-4.10)
- **v1.31.7 · 24.09.2026:** E13 und die Aenderung an find_customer zurueckgenommen (Maxi), Spezifikation wie vor v1.31.6
- **v1.31.6 · 24.09.2026:** E13: KI gibt nie Kundendaten heraus; find_customer ohne Name und Adresse, Adresse nennt der Anrufer, Absage-Satz, Eval-Faelle in T-6.2
- **v1.31.5 · 24.09.2026:** E12 praezisiert, KI-Hinweis bleibt E7 (Art. 50 KI-VO gilt seit 02.08.2026)
- **v1.31.4 · 24.09.2026:** D10 entschieden (90 Tage, CALL_LOG_RETENTION_DAYS), D9 Einschaetzung Eigenbenennung, E12 Wiedererkennung per caller_id
- **v1.31.3 · 24.09.2026:** Anrufprotokoll: Loeschfrist per --frist-tage/--loeschen, kaputte Entwuerfe gemeldet; D9 Datenschutzbeauftragter, D10 Frist offen
- **v1.31.2 · 24.09.2026:** Anrufprotokoll nach Rechts-Check: Entwuerfe ohne echte Kundensaetze (DSFA M15), Allergien einer Person abgelehnt (M8), E10 und E11 entschieden
- **v1.31.1 · 24.09.2026:** Anrufprotokoll: diktierte Nummern, E-Mails und Adressen abgelehnt, richtige Zeilennummern, UTF-8-Meldung, durchgesehene Faelle nie ueberschrieben, Wochentag je Tag
- **v1.31.0 · 24.09.2026:** Anrufprotokoll ohne Tonaufnahme: Druckbogen, CSV-Format (docs/17), scripts/call_log.py fuer C1-Baseline und Eval-Entwuerfe
- **v1.30.1 · 24.09.2026:** Vier Befunde aus PR #130 im Text-Telefon behoben (fuehrende Null keine Menge, Menge bleibt bei neuer Suche, Abholung im spaeteren Satz, jedes ausverkaufte Gericht wird gesagt); dazu das eigene Review: Menge nach der Regel der Domain, gesprochene Nummer waehlt den Vorschlag, `canonical_card` an einer Stelle. Review-Regel verschaerft (Maxi): nach der abgearbeiteten Runde werden P1 noch vor dem Merge gefixt, nur P2 vermerkt
- **v1.30.0 · 24.09.2026:** Projektpflege traegt PR-Felder und Assignee/Label nach
- **v1.29.0 · 24.09.2026:** Projektpflege sammelt offene Entscheidungen in einem Issue mit Erwaehnung
- **v1.28.0 · 24.09.2026:** Abholung im Text-Telefon, Rufnummer aus der Rufnummernerkennung, Verstandenes sofort wiederholen; D8 entschieden (nichts anbieten, was die Karte nicht kennt), T-4.10 angelegt; PR #127 gemergt, zwei P2 als offene Punkte
- **v1.27.2 · 24.09.2026:** Projektpflege bricht ab, wenn der Token das Repo nicht sieht
- **v1.27.1 · 24.09.2026:** Pull Requests ohne Merge werden nicht mehr als erledigt gezaehlt
- **v1.27.0 · 24.09.2026:** GitHub-Project als Ableitung festgelegt und zusammengefuehrt: ein Board (Nr. 2), Status nur aus Merge und Close, taegliche Pflege setzt Abgeschlossenes selbst auf Done, Claude pflegt das Board nur in /project und /gate
- **v1.26.0 · 23.09.2026:** Menue-Tools fuer den Agenten, Prompt v2
- **v1.25.0 · 23.09.2026:** confirm fuer Bestellungen: Abholcode je Betriebstag, Uebergabe nur im Modus primary
- **v1.24.0 · 23.09.2026:** T-4.5 draft_order fuer Abholung, Satz-Zerlegung je Position vor search_menu
- **v1.23.2 · 22.09.2026:** Mount-Drift abgesichert: test_compose_mounts.py prueft die api-Mounts gegen das, was die Suite liest
- **v1.23.1 · 22.09.2026:** docker-compose: api service gets the mounts the test suite reads (sim, Makefile, deploy, .claude, docs, CLAUDE.md, README, CHANGELOG); make test and make lint match CI again
- **v1.23.0 · 22.09.2026:** Review T-4.4: `allergen_question` als Pflichtfeld im Request - der Satz zum Rückruf kommt nur noch auf die Allergenfrage, nicht mehr auf jede Detailabfrage; `confirmed_at` als Ortsdatum statt UTC-Datum
- **v1.22.1 · 22.09.2026:** Rule A measured and recorded: with a number marker a second position is a question, without one the name search takes over and drops the spoken number - one sentence, one position until T-4.5 splits it
- **v1.22.0 · 22.09.2026:** T-4.4 done: get_item_details with allergen rule unknown is not none, shared option building block with search_menu
- **v1.21.3 · 22.09.2026:** PR #120 (Jules review of T-4.3) recorded as an open point: its N+1 finding is obsolete since Rule A, the reviewer journal is still to decide
- **v1.21.2 · 21.09.2026:** Codex review PR #117 (P2) fixed: trigram prefilter is a provable superset of the score cutoff
- **v1.21.1 · 21.09.2026:** Codex review PR #117 (P2) fixed: only card letters a-f count as a number prefix, so a short everyday word asks back
- **v1.21.0 · 21.09.2026:** Code review PR #117: both number exits agree, card letters bounded on both spellings, ambiguous contract documented, one session per SSE tick
- **v1.20.6 · 21.09.2026:** CI red fixed: a marker without its own number only counts for a number that follows it
- **v1.20.5 · 21.09.2026:** Codex review PR #117 (P2 x2) fixed: card letters bounded to two, a marker with a number beside a name asks back
- **v1.20.4 · 21.09.2026:** Codex review PR #117 (P1, P2) fixed: letter before a spoken number word, separated multi-letter card suffix
- **v1.20.3 · 21.09.2026:** Codex review PR #117 (P2) fixed: an alias made only of filler words is an import error, it could never be found
- **v1.20.2 · 21.09.2026:** Codex review PR #117 (P2 x2) fixed: alias collisions warned by search identity, fuzzy search prefiltered through the GIN index
- **v1.20.1 · 21.09.2026:** Codex review PR #117 (P2) fixed: both number exits share one marker rule, eval set guards the agreement
- **v1.20.0 · 21.09.2026:** Eval set for number recognition: 64 cases without DB or model, in CI, catches the regressions of the last eight review rounds
- **v1.19.24 · 21.09.2026:** Codex review PR #117 (P2 x2) fixed: quantity plus name with a connector stays name search, separated multi-letter prefix stays a number
- **v1.19.23 · 21.09.2026:** Codex review PR #117 (P1) fixed: a connector word without a second number makes the sentence a question, not an order
- **v1.19.22 · 21.09.2026:** Codex review PR #117 (P1, P2) fixed: marker survives filler and hesitation, separated letter before digits stays a number
- **v1.19.21 · 21.09.2026:** Codex review PR #117 (P2) fixed: compound number words like eintausend are recognized as out of range
- **v1.19.20 · 21.09.2026:** Codex review PR #117 (P1) fixed: a letter before the digits after a marker is a number that does not exist, not a dish name
- **v1.19.19 · 21.09.2026:** Codex review PR #117 (P2 x2) fixed: filler words without umlauts stripped, oversized number word after a marker stays a number
- **v1.19.18 · 21.09.2026:** Codex review PR #117 (P1) fixed: two numbers without a marker ask back instead of searching for the connector word
- **v1.19.17 · 21.09.2026:** Rule A (Maxi): search_menu takes a number only when the whole sentence is that one number, otherwise it asks back
- **v1.19.16 · 18.09.2026:** Codex review PR #117 (P2 x2) fixed: connector must sit directly between numbers, alias like 7up kept
- **v1.19.15 · 18.09.2026:** Codex review PR #117 (P1) fixed: hesitation between two numbers asks back
- **v1.19.14 · 18.09.2026:** Codex review PR #117 (P2) fixed: filler words allowed between number marker and number
- **v1.19.13 · 18.09.2026:** Codex review PR #117 (P1, P2) fixed: out-of-range marked number stays invalid, import dedupes 7 and 07
- **v1.19.12 · 18.09.2026:** Codex review PR #117 (P1) fixed: und and comma-separated second numbers ask back
- **v1.19.11 · 18.09.2026:** Codex review PR #117 (P1, P2) fixed: adjacent numbers ask back, correction word scoped to the preceding number
- **v1.19.10 · 18.09.2026:** Codex review PR #117 (P1, P2) fixed: separated x invalid after marker, alternatives need a connector word
- **v1.19.9 · 18.09.2026:** Codex review PR #117 (P1, P2) fixed: 23x invalid after marker, legacy case duplicates stop the import
- **v1.19.8 · 18.09.2026:** Codex review PR #117 (P1) fixed: alternative after a single number marker asks back
- **v1.19.7 · 18.09.2026:** Codex review PR #117 (P2 x3) fixed: legacy mixed-case numbers reconciled, number residue ignored, canonical dedupe
- **v1.19.6 · 18.09.2026:** Codex review PR #117 (P1) fixed: unsupported suffix makes the number invalid instead of truncating
- **v1.19.5 · 18.09.2026:** Codex review PR #117 (P1) fixed: two marked numbers in one sentence ask back
- **v1.19.4 · 18.09.2026:** Codex review PR #117 (P2 x2) fixed: card numbers up to 999, suffix stored lowercase
- **v1.19.3 · 18.09.2026:** Codex review PR #117 (P1, P2) fixed: import restricts card numbers to the searchable format, compact quantities stripped
- **v1.19.2 · 18.09.2026:** Codex review PR #117 (P1) fixed: number, suffix and marker from one span
- **v1.19.1 · 18.09.2026:** Codex review PR #116 (P1) fixed: card numbers matched as text, 23a and leading zeros
- **v1.19.0 · 18.09.2026:** T-4.3 done: search_menu tool with number, alias and trigram resolution, never guesses
- **v1.18.1 · 18.09.2026:** Codex review PR #115 (P2 x2) fixed: allergen confirmer re-stamped on change, dry-run price report says would be applied
- **v1.18.0 · 18.09.2026:** T-4.2 done as code: menu import with all docs/14 check rules, dry-run, idempotent; real CSVs pending from chat
- **v1.17.2 · 18.09.2026:** Codex review PR #114 (P2 x2) fixed: order_items tenant-scoped via composite FKs, migration tests self-contained
- **v1.17.1 · 18.09.2026:** Review PR #114 fixed: allergen_code restricted to the 14 LMIV letters
- **v1.17.0 · 18.09.2026:** T-4.1 done: migration 002 with menu and order tables, pg_trgm, DB-side checks for money and totals
- **v1.16.2 · 18.09.2026:** Design pass operations view: 18 px for notes/badges, readable primary button in dark mode, callback cards restructured, column counts
- **v1.16.1 · 18.09.2026:** Codex review PR #112 (P1) fixed: audio context unlocked in the first tap for tablet Safari
- **v1.16.0 · 18.09.2026:** T-3.4 done: callbacks column with tone, complaints on top, done button; event stream signal callbacks
- **v1.15.1 · 18.09.2026:** Codex review PR #111 (P2 x2) fixed: header token includes shown values, app.js swaps only HTML error responses
- **v1.15.0 · 18.09.2026:** T-3.2 done: header buttons (pause AI, resume with confirmation, delivery on/off, wait time +15/+30) and live header via SSE
- **v1.14.1 · 18.09.2026:** Latenztests: p95_ms misst bis zu 3 Reihen, p95 über alle bisherigen Aufrufe (keine Reihe verworfen), Budget bleibt 300 ms lokal und in CI (docs/04 §1)
- **v1.14.0 · 17.09.2026:** T-0.7 done: slash commands played through, CI steps green, gate.md reference and CI branch filter fixed, test_commands.py added
- **v1.13.0 · 17.09.2026:** T-3.1 and T-3.3 done (#108): operations view with column Heute and live updates, milestone "Durchstich ohne Telefon" closed
- **v1.10.1 · 17.09.2026:** Codex review PR #102 (P2 x3) fixed: understanding_failure deduped per customer turn, storno as a noun recognized, escalation callbacks carry the triggering customer text
- **v1.10.0 · 17.09.2026:** T-2.2 done: agent/ladder.py + escalation.py, wired into loop.py; leads to T-2.3 first terminal conversation
- **v1.9.1 · 17.09.2026:** Codex review PR #101 (P1 x3, P2 x2) fixed: state_patch on LLMTurn so extracted slot values survive to the next turn, reservation_id exposed in prompt state for confirm, max_call_seconds/hop-limit abort now really hands off to transfer_to_team/create_callback instead of only announcing it, deadline rechecked every tool hop, tool say forwarded to the model
- **v1.9.0 · 17.09.2026:** T-2.1 done: agent/ Gesprächs-Kern (prompt, state, loop, dispatch, Fake-LLM); nächster Schritt T-2.2 Verständnis-Leiter
- **v1.8.0 · 17.09.2026:** T-1.10 done: prompts/system_v1.md and tools_v1.md, scoped to Stage 1's live tools
- **v1.7.1 · 17.09.2026:** Codex review PR #99 (P1, P2) fixed: tool_calls update scoped to tenant_id, start_call serialized per external_session_id
- **v1.7.0 · 17.09.2026:** T-1.9 done: call log (POST /v1/calls/start, /end) plus tool_calls recording for every tool call
- **v1.6.4 · 17.09.2026:** Codex review PR #98 (P1, P2) fixed: dedicated TransferReason with cancellation, unavailable transfer_to_team no longer marks the call as transferred
- **v1.6.3 · 17.09.2026:** T-1.8 done: transfer_to_team tool with team_phone, availability check and once-per-call loop protection
- **v1.6.2 · 17.09.2026:** Documented lock contract for callbacks, READ COMMITTED set in api/db.py, overlap and rollback test added (Codex review PR #95)
- **v1.6.1 · 17.09.2026:** create_callback: advisory lock per call against double callbacks on concurrent first calls (Codex review PR #95, P1)
- **v1.6.0 · 17.09.2026:** T-1.7 done: create_callback with state idempotency, audit_log and outbox event; bugfix in normalize_phone (bracketed zero); next step T-1.8
- **v1.5.2 · 17.09.2026:** Dependabot no longer bumps container Python version alone (PR #82 closed); 3.12 stays set until deliberate upgrade
- **v1.5.1 · 17.09.2026:** Lint rules fixed in pyproject.toml (select list, extend-immutable-calls for FastAPI Depends); findings from ruff 0.16 fixed so PR #85 can merge green
- **v1.5.0 · 17.09.2026:** T-1.12 done: events/ with outbox writer and dispatcher to n8n, backoff and alarm, own service; migrations no longer silence app logger; next steps T-1.7 through T-1.9
- **v1.4.1 · 17.09.2026:** Codex review PR #90 (P1) fixed: confirm requires same call as draft, red test first
- **v1.4.0 · 17.09.2026:** T-1.6 done: confirm generic, draft to confirmed with row lock, audit_log and outbox event reservation.confirmed; next step T-1.12 dispatcher
- **v1.3.3 · 16.09.2026:** Repo standards: CONTRIBUTING, SECURITY, PR and issue templates, Dependabot, README badges; label taxonomy on all 77 issues
- **v1.3.2 · 16.09.2026:** Roadmap mirrored to GitHub: nine block issues, 68 work packages as sub-issues, issue numbers entered in docs/07
- **v1.3.1 · 16.09.2026:** Codex review PR #4 (P1, P2) fixed: lock against overbooking on parallel calls, readback stable past midnight
- **v1.3.0 · 16.09.2026:** T-1.5 done: create_reservation as draft with readback, idempotency and audit_log; core/ids.py and E.164 normalization new; next steps T-1.6, T-1.12, T-1.7 through T-1.9
- **v1.2.0 · 16.09.2026:** Merged PR #2: T-1.1 through T-1.4 done (core/, Alembic, seed, get_service_status, check_slot), rebrand Maex Voice-Agent with placeholders, README strategy + CHANGELOG.md + /gate introduced, Codex review fixes
- **v1.1.1 · 16.09.2026:** Roadmap section, versioning schema and auto-update (script + CI sync check) introduced
- **v1.1.0 · 16.09.2026:** Bundle v1.1 – magnifying glass over the plan: modules, playbooks, deployment, import format, outbox, agent core, D7
- **v1.0.0 · 15.09.2026:** First version at export to Claude Code

Older, non-semver entries (T-0.1 through T-1.4, rebrand, README strategy) stand chronologically in the done table above.
